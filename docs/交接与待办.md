# 交接与待办

> 换电脑继续开发时先读这一份。
> 最后更新：2026-08-19 · 对应提交 `公共部门货物采购官：4,433 位真实联系人`

---

## 一、换机后第一件事：私钥不在仓库里

**`tools/keys/private.pem` 被 `.gitignore` 排除，clone 下来是没有的。**

它是**全世界唯一**能签出有效激活码的文件。新电脑上没有它：

- ❌ `node tools/gen-key.mjs` 发不出激活码
- ❌ `npm run test:desktop` 会失败（签发/验签对不上）
- ✅ 软件本身照常跑（验签用的是硬编码在 `electron/license.js` 里的公钥）

### 怎么办

**正确做法：把老电脑上的 `tools/keys/private.pem` 用 U 盘拷过来**，放回同样路径。
不要走微信、网盘、邮件——这个文件泄露等于任何人都能给自己发激活码。

**千万别做的事：在新电脑上跑 `node tools/init-keys.mjs --force`。**
那会生成一对新密钥并改写 `electron/license.js` 里的公钥，结果是
**所有已经发出去的激活码全部失效**。这个坑本项目已经踩过一次。

### 现在有几份（2026-08-06）

开发机上另外放了两份（权限已收紧为仅本人可读写，各带一份说明 txt）：

- `C:\Users\<用户名>\AppData\Roaming\觅客舵-密钥备份\private.pem`
- `E:\觅客舵-密钥备份\private.pem`

怎么确认某一份没坏、且是对的那一把——推导公钥，必须等于
`electron/license.js` 里的 `PUBLIC_KEY_B64`（对不上会导致 `test-desktop`
全挂，历史上栽过一次）：

```bash
node -e "const c=require('crypto'),f=require('fs');const p=c.createPrivateKey(f.readFileSync('tools/keys/private.pem','utf8'));const d=c.createPublicKey(p).export({type:'spki',format:'der'});console.log(d.subarray(d.length-32).toString('base64'))"
```

> ⚠️ **仍未解决：这三份在同一块物理硬盘上**（开发机的 C/D/E 是同一块
> Samsung 870 EVO）。所以现在只防得住误删和误 `git clean`，**防不住硬盘损坏**。
> 脱机备份（U 盘）当时手边没有可移动介质，**仍待补**。
>
> 明确不走网盘：机器上的 OneDrive 是学校账号，明文签名私钥不上第三方云端；
> 真要走网盘必须先加密再传。

---

## 二、换机环境准备

```bash
git clone https://github.com/tongyanzhanggithub/mikeduo.git
cd mikeduo
npm install
node build.mjs
npm start
```

要注意的几点：

| 项目 | 说明 |
|---|---|
| Node | 24.x（开发时用的版本） |
| 平台 | Windows x64。`electron/machine-code.js` 用了 wmic/PowerShell，换 macOS 要另写 |
| `app.js` / `index.html` | **构建产物**，虽然进了 git 但不要手改。改 `src/*.js` 后跑 `node build.mjs`。版本戳已做行尾归一化，换机构建不会平白变脏（提交 `255dfaf`） |
| API Key | 存在本机 userData，不进仓库也不进备份。新电脑要重新填 |
| 业务数据 | 在 `%APPDATA%\觅客舵`。要带走就用「设置 → 导出全量备份」 |

### 跑测试

```bash
node tools/test-send-guard.mjs && node tools/test-license.mjs && node tools/test-parse.mjs && node tools/test-import.mjs && node tools/test-netprobe.mjs && node tools/test-source-hygiene.mjs && node tools/test-update-gate.mjs && node tools/test-build-stamp.mjs && node tools/test-screening.mjs && node tools/test-hscode.mjs && node tools/test-tenders.mjs && node tools/test-dataset-age.mjs && node tools/test-journey.mjs
```

```bash
npm run test:customs && npm run test:mail && npm run test:desktop
```

后三个必须用 electron 跑（要 safeStorage / 真实文件系统），直接 `node` 会报错。

16 个套件共 **223 项**，全绿才算基线正常（2026-08-19 实测）：

| 套件 | 项数 | 覆盖 |
|---|---|---|
| `test-netprobe` | 57 | 官网抓取、F3 判定优先级、行为分支、定位校准 |
| `test-send-guard` | 22 | 未验证拦截、试用闸门、合规、脱敏 |
| `test-mail` | 19 | 内置 SMTP/IMAP、退信 DSN |
| `test-screening` | 15 | 制裁名单归一化匹配、CMIC 不是禁令、三条法律线分别说后果 |
| `test-customs` | 13 | 本地提单库 |
| `test-tenders` | 13 | 采购官库；前四条测的是**诚实性**不是功能 |
| `test-parse` / `test-import` | 11 / 11 | AI 返回解析 / 粘贴导入 |
| `test-hscode` | 11 | HS 校验与错码分型 |
| `test-license` | 10 | 激活码签发验签 |
| `test-dataset-age` | 11 | 内置数据集陈旧度判定 |
| `test-build-stamp` | 8 | 构建过期检测（版本戳不在前 4096 字节） |
| `test-update-gate` | 7 | 占位符更新源不得带进发布版 |
| `test-desktop` | 7 | 真 Electron：机器码 / safeStorage / 备份 |
| `test-journey` | 6 | 端到端旅程：串起来能不能走通 |
| `test-source-hygiene` | 2 | 来源卫生 |

### 内置数据集怎么重建

`data/` 下三份 gz 是从一份外部数据集抽出来的，**原库不在仓库里**
（395 MB，路径 `D:\project\海关数据\exports	rade_dataset.sqlite`）。
换机后如果那个库不在，这三份文件照常能用（已提交进仓库），只有要更新时才需要原库：

```bash
node tools/build-screening.mjs   # 制裁名单 → data/screening.json.gz  452 KB
node tools/build-hscodes.mjs     # HS 目录   → data/hscodes.json.gz    158 KB
node tools/build-tenders.mjs     # 采购官库  → data/tenders.json.gz    515 KB
```

三个脚本都要本机有 python（只是拿它读 sqlite，用户端不需要）。
**制裁名单是持续变动的**，重大交易前仍应到官方站点复核——这句话在界面上也写着。

## 三、没做完的事（按重要性排）

### 🔴 P0：会影响能不能卖

#### 1. ~~官网抓取的真实覆盖率没验过~~ ✅ 已实测（2026-08-19）

`node tools/coverage-test.mjs 清单.txt`（输入格式随便，含粘贴的搜索结果）。

20 个真实海外买家官网，用产品自己的方法生成，6 品类 × 6 市场：

| 指标 | 实测 |
|---|---|
| 域名能打开 | 80% |
| 拿到邮箱（占能打开的） | **68.8%** ← README 写的 ≥60% 站得住 |
| 拿到同域邮箱 | 62.5% |
| 拿到电话 | 81.3% |
| 拿到 WhatsApp | 43.8% |
| 邮箱或电话至少一个 | **93.8%** |
| 动态渲染抓不到的站 | **0%** |

**样本只有 20 个，置信度有限。** 有真实客户名单时应该再跑一次 50–100 个确认。

三个直接影响决策的发现：

1. **电话比邮箱更容易拿到**（81.3% vs 68.8%），WhatsApp 43.8%。产品叙事不该
   只讲邮箱——中小买家官网留电话比留邮箱普遍，而外贸本来就重 WhatsApp。
2. **一个 SPA 站都没有** → 第 2 条里说的「隐藏 BrowserWindow 渲染」**不值得做**。
   发展中市场的中小进口商官网基本是老派静态 HTML。
3. 跑这次实测时抓出一个真 bug：电话提取把 `20250909`（日期）和 `00512512`（编号）
   当成了号码，「拿到电话」虚高到 87.5%。修完是 81.3%。
   **不实测就会拿着虚高的数字去写销售页。**

#### 2. SPA 官网 —— ✅ 已如实告知（2026-08-19），彻底解决仍未做

**已做**：抓取时判断这一页是不是 JS 渲染出来的空壳，并据此说两句完全不同的话。

- 判据：框架特征（空的 `#root`/`#__next` 容器、`__NEXT_DATA__`、Nuxt、Angular、
  React、Wix、Shopify、Squarespace）+ 正文长度；另有一条兜底（正文极少而脚本很多）
- **服务端渲染过的 React 不会被误判**——有框架特征但正文够多就照常抓
- 空壳站的提示改成：「官网是动态渲染的（…），内容要浏览器跑完脚本才出现，
  我们抓到的是空壳——**这不代表对方没公示联系方式**。建议手动打开看一眼，
  或改用 Hunter / 邮箱查找 Webhook。」
- 批量抓取的汇总里空壳站单独计数，不混进"没抓到"

原来那句「抓了 N 页，没有公示邮箱」会让用户以为是对方没写，**顺手把好线索删掉**。

**仍未做（真正的解决）**：用 Electron 的隐藏 `BrowserWindow` 加载页面、等 JS 跑完
再取 DOM。技术上可行（约 60 行），但要处理超时、内存、弹窗、下载拦截，
并且要串行化以免同时开一堆窗口。**只在检测到 spa 时才触发**，成本可控。
做不做取决于第 1 条（覆盖率实测）的结果——如果空壳站占比很低，就不值得做。

#### 3. ~~RCPT 探测没在真实网络下跑通~~ ✅ 已实测（2026-08-21）

**先纠正一个错判**：这条待办原来写「开发机出站 25 端口被封」，那是错的。
当时是**沙箱 DNS 解析不出 MX**，我把它误判成端口封锁。实测 25 端口是通的
（gmail / qq 都能握手）。

实测跑通后暴露两个真 bug，都属于「把测不出说成了确定结论」这一类：

**① MX 查询还在用裸的系统解析器** ——`domainHealth` 早就改成多解析器了，
`verifyEmail` 漏掉了。结果 `github.com` / `qq.com` 被判成「没有 MX 记录，
收不了信」。而 `invalid` 在 F3 里是**一票否决**：DNS 一抖，用户所有邮件
都会被拦，理由还是错的。现在走 `mxChecked`，并区分「有解析器明确说没有」
（invalid）与「谁都没答上来」（unknown）。

**② 550 不等于 550。** 真网实测拿到两条原文：

```
github.com → 550 5.7.1 ... Client host [x.x.x.x] blocked using Spamhaus
qq.com     → 550 Mailbox not found
```

前者是「你的 IP 被拉黑了，我不跟你说话」，跟地址存不存在毫无关系；
后者才是「这个地址不存在」。原实现只看状态码，会把前者判成 invalid，
**把真客户的地址拉黑**。现在按应答正文分类：blocked / invalid / unknown。

**③ 一个必须知道的现实**：家用与办公宽带的出口 IP 常年在 Spamhaus 之类的
公共黑名单上（开发机就是），所以对大厂邮箱的探测多数会返回 blocked。
这不是 bug，是这条路的固有局限。现在批量探测结束时会把原因讲一次：
「你的出口 IP 在 Spamhaus 黑名单上……这不是地址有问题，也不是软件坏了」，
并给出替代方案（配 Hunter / 换出口线路）。

**结论**：RCPT 探测对自建域名和中小企业邮箱有效，对大厂邮箱多数测不出。
它的定位应该是「能测出来的算白赚，测不出不影响原有判定」，
而不是主要的验证手段——README 与界面措辞已按这个口径。

#### 4. ~~「零配置」这个说法兑现不了~~ ✅ 已改叙事（2026-08-21）

**结论：不再宣称「零配置」，改成把两段式现实和真实价钱摊开写。**

因为「零配置」这句话其实**对第一封信是成立的**——粘贴 Google 搜索结果 +
抓官网拿联系方式 + 填自己的 SMTP，一个 key 都不用。上手向导的五步就是这条路。
不成立的是把它泛化到整个产品：跑量要 SerpAPI，AI 写信读信要模型 key。

含糊过去的后果是用户走完五步之后**突然撞上收费墙**。所以现在：

- 向导副标题限定范围：「这五步不用配任何 API key……要跑量、要 AI 写信才需要配」
- 向导底部新增成本表，逐项写清免费/收费，AI 那行直接写 **≈¥14/月**
  并给 DeepSeek 与 SerpAPI 的申请链接，旁边显示「已配/未配」
- 明说没有「内置额度」，并给理由：`.asar` 可解包，内置我们的 key 等于送给所有人
- README 新增「要花多少钱」一节，同一套口径

`test-netprobe` 加了三条测试守住这件事：成本表不许被删、AI 月成本必须写出来、
副标题必须限定在「这五步」而不能泛化成整个产品。

**仍然不建议的两条路**（架构上冲突，不是没想到）：
- 自建中转垫额度 → 就有服务端了，和「纯本地 + 买断」的定位冲突
- 内置我们自己的 key → asar 可解包

### ✅ P0 已全部清空（2026-08-21）

四条 P0 都有结论了。其中三条的结论是**实测推翻了原来的假设**：

| 原假设 | 实测结论 |
|---|---|
| 覆盖率不明，可能只有 20-30% | 68.8% 拿到邮箱、93.8% 至少一个联系方式 |
| SPA 站抓不到，可能要做浏览器渲染 | 真实客群里 **0% 是 SPA**，那块工作量直接省掉 |
| 开发机 25 端口被封，测不了 RCPT | 端口是通的；跑通后反而**修出两个真 bug** |
| 追求零配置 | 第一封信本来就零配置，问题是没说清后面要花钱 |

**教训：三条 P0 里有三条的原始判断是错的。** 遇到「据说不行」的事，
先花十分钟真测一次，比基于假设做设计划算得多。

### 🟠 P1：功能不完整

#### 5. ~~追踪像素只有发射端，没有接收端~~ ✅ 已补齐（2026-08-21）

给了两份能直接粘的端点实现 + 一份搭建文档，都随包发货：

- `docs/tracking/cloudflare-worker.js`（推荐，免费额度每天 10 万次请求）
- `docs/tracking/vercel-api-track.js`
- `docs/邮件打开追踪怎么搭.md`——十分钟走完，含验证步骤

设置卡里直接有「看怎么搭」按钮，并提醒**要填两个框**（追踪端点 + 发送状态
回传 Webhook），只填一个是收不到数据的——这是最容易漏的一步。

两个实现上的决定：

- **像素 URL 只带 outbox id，绝不带收件人邮箱。** 邮件源码是收件人可见的，
  中间每一跳也可见；把邮箱写进 URL 等于当面泄漏，还会被反垃圾系统当成
  典型追踪特征。为此把事件匹配从「只认邮箱」扩成「id 优先、邮箱兜底」。
- **像素路径不校验 token，拉取路径必须校验。** 邮件客户端请求像素时带不了
  token；但 `/events` 不校验的话，任何人都能拉走你客户的打开记录。

文档里如实写了打开率天然不准（Gmail/Outlook 预取会虚高、客户端屏蔽图片会
虚低），以及它在软件里只用于「打开 ≥3 次未回复」的排序——这个用法对误差不敏感。

端到端验过：起一个本地端点，像素请求 3 次 → 拉取回 3 条事件 → 打开次数 3 →
行为分支正确落到「打开 ≥3 次但没回」。无 token 拉取返回 403。

#### 6. ~~开发信引用只覆盖英文首封~~ ✅ 已多语种化（2026-08-21）

原来引用句是写死的英文。非英语市场（西/葡/阿/法/俄/越/印尼/土）走 `localIntro`
分支，结果那封信**开头是当地语言、中间突然夹一句英文**——比不做个性化还糟。

现在按 `marketLanguage()` 出九个语种的对应句式。**语种没覆盖到就不插入**，
而不是退回英文——夹一句看不懂的话是负分。

#### 7. ~~抓到的"事实"太浅~~ ✅ 已加深（2026-08-21）

原来只取 `<title>` / `meta description` / `h1-h3`，抓到的几乎都是
「我们是一家专业的XX公司」。**引用这种话反而暴露你是群发的。**

现在多抓五类只有认真看过这个站才知道的东西：

| 类型 | 例子 |
|---|---|
| 代理品牌 | authorized distributor of John Deere parts in the Middle East |
| 资质认证 | ISO 9001:2015、CE marking、RoHS |
| 近期动态 | New warehouse opened in 2024 |
| 经营年头 | Established in 2004 |
| 服务区域 | serving UAE, Saudi Arabia, Oman and Kuwait |

红线不变：**只摘录原文，不概括不推断**，每条都能在页面上原样找到。

两个配套的判断：

- 抓取端按具体程度排序，代理品牌排最前、标题排最后
- **只剩标题/描述这类泛泛的就不引用**——塞一句「贵司是专业供应商」比不说更糟。
  实测「只有泛泛介绍」的站返回空，不硬凑

两处贪婪匹配也收紧了：品牌名不许跨句（原来会把下一句的 ISO 也吃进来）、
服务区域遇到介词就停。版权行不会被当成近期动态。

#### 8. ~~试用版下这些新能力没有额度限制~~ ✅ 已拍板：不限（2026-08-19）

**决定：官网抓取 / RCPT 邮箱验证 / 域名体检 / 合规筛查 / HS 校验 / 采购官库检索，
试用版一律不设次数限制。**

理由写在 `src/0-brand-edition.js` 的 `TRIAL_LEAD_CAP` 旁边（改之前先读那段）：

- 这六样全在本机跑，一分钱接口费都不花，限制了我们什么成本都省不下来
- 更重要的是产品处境——我们真正的优势（拦退信、防域名被废、不编造数据）
  **全是滞后显现的**，用户试用时只体验到"这软件老拦着我不让发"；
  而劣势（要自己配 key、自己找线索）第一小时就撞上。
  这六样恰恰是**即时能感受到价值**的，用它们把体验前置，正好补这个短板
- 唯一权衡过的是采购官库（4,433 个真实联系人，理论上有人只为拿名单而下载），
  但导入仍受 20 条线索池上限约束，拿不走多少

配套动作：升级墙里主动把这六样列出来告诉用户"不限次，随便用"；
`test-send-guard.mjs` 加了一条测试，守住"配额表里只该有按次计费的
SerpAPI / Hunter"，防止以后被顺手加上配额。

### 🟡 P2：该做但不急

#### 9. ~~内置数据集会过期，但没有更新机制~~ ✅ 已解决（2026-08-19）

做法是「随版本重建 + 应用内陈旧提示」：

- `node tools/check-data-age.mjs` 是发版硬前置，超过告警线退出码为 1
- 阈值按各自更新频率分开定：制裁名单 90/180 天，采购官库 180/365 天，
  HS 目录 730/1460 天
- 应用内三个面板都会显示陈旧提醒，制裁名单**即使未命中也会露出提醒**
  ——那恰恰是最危险的一刻（用户看到"没问题"就放心发了）

**遗留**：重建要用原库（395 MB，不在仓库里）+ 本机 python。
没有原库就发不了带新名单的版。如果以后换人维护，这是个卡点。

#### 10. 采购官库只覆盖世行融资项目

4,433 位全部来自世行融资项目的公开采购公告，也就是发展中国家公共部门与国际组织。
亚开行、非开行、欧盟对外援助、各国政府自己的采购平台都没有覆盖。

**要不要扩**取决于第 9 条（竞品实测）的结论——如果公共部门这条路真能出单，
再考虑接更多来源；如果只是"看着数据多"，就到此为止。

#### 11. 竞品实测还没做

上一轮建议过：花 159 元买一个月 ProscaApp，**重点只看一件事——
它给的邮箱往里发一封，退信率多少**。

如果它的联系人也是拼的，那是整个定位可以立起来的支点（"别人给你 3 亿条
待退信的名单，我们给你能发到的"）。如果是真的，我们得重新想差异化。

**这件事的结论会决定接下来做什么，值得优先。**

#### 12. 发版流程：打包已验证，发布与自动更新仍未验证

**已经验证过的（2026-08-15，实际跑通 `npm run dist`）：**

- 安装包能打出来：`dist-desktop/MiKeDuo-Setup-v1.0.0.exe`，约 80 MB
- nodemailer / imapflow / electron-updater 都进了 `app.asar`，且能从 asar 里
  正常 require（收发信是在函数体内才 require 的，应用能启动并不能证明这一点，
  所以单独跑了一次 asar 内加载验证）
- 整个包里没有原生模块（`.node`），因此不需要 `asarUnpack`
- 托盘图标从 asar 里读得出来（256×256），启动不再往 main-error.log 写东西
- 打包后的应用能正常启动并跑起渲染层

**Windows 上第一次打包会失败**，报 `Cannot create symbolic link ... 客户端没有
所需的特权`：electron-builder 解压 winCodeSign 时要建 macOS 用的软链接，
Windows 普通用户没这个权限。两种解法，任选其一：

1. 打开「设置 → 隐私和安全性 → 开发者选项 → 开发人员模式」，之后正常打包
2. 手工把缓存解出来（跳过 macOS 那部分，Windows 构建根本用不到）：

   ```
   node_modules/7zip-bin/win/x64/7za.exe x -bd -y \
     "%LOCALAPPDATA%/electron-builder/Cache/winCodeSign/<任一>.7z" \
     "-o%LOCALAPPDATA%/electron-builder/Cache/winCodeSign/winCodeSign-2.6.0" \
     "-x!darwin"
   ```

**仍然没有验证的：** 上传 Release → 老版本检测到更新 → 装上去数据还在。
这一段要等 `publish.owner` 配成真实账号、真发一次 Release 才能测。

**安装包目前没有代码签名**（打包日志里的 `no signing info identified`）。
未签名的 exe 在 Windows 上会弹 SmartScreen「未知发布者」警告，买了软件的人
第一次装就看到这个，退款率和客服量都会受影响。要么买一张代码签名证书，
要么在购买页和安装指引里提前说明怎么点「仍要运行」。

#### 13. 发货前必须替换的占位

见 README 的「发货前必须替换的占位」一节：客服微信、购买链接、
课程链接、公司主体信息等，现在都还是占位符。

---

## 四、代码走查

```bash
node tools/audit.mjs
```

查的都是**静默失效**类问题——不报错、看着对、就是不工作。发现问题退出码为 1。

八项：同名函数重复定义 / 「包一层」包错名字 / 死代码 / `elements.xxx` 指向不存在的
DOM id / IPC 三方对不上 / 线索字段写了没人读 / 源码里的控制字符 /
`admitProspects` 之后必须同步写回 `state.prospects`。

最后一项是个容易被后人破坏的真不变量：`admitProspects` 内部会触发入池体检
（抓官网核实公司存在 + 合规筛查），那段是 fire-and-forget 的微任务，
**依赖调用方在同一个同步块里把线索写进 `state.prospects`**。
中间插一个 `await`，微任务就会先跑，那时线索还不在池子里，体检什么都找不到
——而且不报任何错。

**首次跑就抓出五个真问题，全是「功能做了一半」：**

| 问题 | 后果 |
|---|---|
| `screeningPanelHtml` 写好了从没挂载 | 合规命中详情**永远看不到**；拦是拦住了，但用户不知道命中哪条法律线 |
| `renderCommerceChrome` 没有 try | 它一抛错，上面九个面板**全部不渲染**，而且是静默的 |
| `sequencePausedReason` 只写不读 | 序列悄悄停了，用户既看不到状态也不知道原因 |
| `websiteConfidence` / `websiteNote` 只写不读 | 「AI 推测」和「联网核实」在界面上长得一样，用户没法判断该不该信 |
| `test-import.mjs` 里的 `capitalize` 桩含控制字符 | 那个桩**什么都没做**，等于测试跑的不是真逻辑 |

顺带把 09 里的**六层 `render` 包装合并成一层挂载表**。层数多有两个坏处：
读代码要顺着六层才知道一次渲染干了什么；更要命的是任何一层在 try 外面抛错，
上面所有层都不再执行——而这种失败没有任何提示。

### 第二轮深挖（2026-08-21）：状态机 / 数值口径 / 异步竞态

这三类查完**没有发现新的真问题**，但过程本身值得记下来，省得以后重查：

**状态机**——把所有被写入的 `status` 值和所有被判断的值两边对齐，
四组「判断了但没人写」全是假阳性：`invalid`/`valid` 由 `electron/netprobe.js` 写、
`level` 的三个值由 `dataset-age.js` 写（跨进程，扫 `src/` 看不到），
`all`/`done` 是 index.html 里的下拉选项值。

**唯一需要处理的**：`contactSource === "claude" / "local"` 现在没有任何代码会写出来
——那两条编造联系人的路径已经删了。但老用户数据里还有，所以这条判断必须留着。
已在代码里写了注释，防止被当死代码删掉。

**数值口径**——全项目 10 处过滤 `status === "已发送"`，写法完全一致，
且都不排除退信。这是对的：退信也占用了发送额度和预热配额，
而退信率的分母本来就该是「发出去的总数」。

**异步竞态**——12 个 `admitProspects` 调用点全部同步写回（已固化成第 ⑧ 项检查）。
另外三处「快照跨 await 写回」的疑似模式，逐个看下来都安全：
`probeProspectEmail` / `screenProspect` 写回时是 `state.prospects.map()`
**重新读当前 state**，快照只用来读 `.email`；`findLookalike` 里那个 `await`
在 `if (aiEnabled())` 分支中直接 return 了，写 state 的代码根本到不了。

> 第 ⑦ 项（控制字符）是被自己坑出来的：写这个走查工具时，
> `//` 又被吃成了 `U+0008`，导致 ④ 误报 200 多处。
> **本项目已经栽过六次**。规律：`` `` `
` 这类本身是合法转义的序列会被吃掉，
> `\s` `\d` `\W` 不会。写正则时优先用字符类，或者干脆用 `String.fromCharCode`。

### 第三轮（2026-08-22）：规模性能 —— 线索池上千条会不会卡

**结论：以前会，现在不会了。** 真机实测（Chrome，`node tools/bench-scale.mjs` 是配套的离线基准）：

| 视图 | 1000 条 | 2000 条 | 5000 条 |
|---|---|---|---|
| 工作台 | 92 → **12ms** | 347 → **16ms** | 2465 → **28ms** |
| 潜客页 | 290 → **63ms** | 614 → **60ms** | 1644 → **68ms** |
| CRM 看板 | 223 → **24ms** | — → **26ms** | 1344 → **32ms** |
| 分析页 | 41 → **28ms** | — → **54ms** | 526 → **211ms** |
| 发信队列 | 111 → **60ms** | 232 → **78ms** | 623 → **136ms** |
| 收件箱 | 82 → **32ms** | — → **36ms** | 459 → **41ms** |

问题分两类，修法完全不同。

**第一类：算法平方级。** 「这条线索回过信没 / 开过信没 / 发过没 / 对应的线索在哪」
这几个问题被反复问，而每问一次就 `some()`/`find()` 扫一遍全表，问的次数又跟线索数成正比。
`isReplied` 在一次渲染里被调 3465 次，每次扫一遍全部来信。修掉六处：

- `computeLeadScore` 的互动信号（3 处内联扫表 + `hasSentOutbound`）
- `preflightOutboxItem` —— **三层各查一遍同一条线索**（基础实现 + 08 的 F3 包装 + 09 的合规包装），
  「包一层」这个模式的固有代价：每层都独立重新查一次
- `funnelFor` —— 五个 `prospects.filter(p => outbox.some(...))`。注释写着「漏斗算法只此一份」，
  所以这一处平方级同时拖慢分析页、渠道对比、来源效果、转化协助一整片
- `isReplied` / `replyChannels`
- `dueFollowupProspects`（工作台待办每次都调）
- `active*` / `ax*` 那一族派生集合 —— 86 个渲染函数都在调，每次重新过一遍全池

做法是**渲染期索引**（`src/0-brand-edition.js` 的 `buildScanIndex` / `renderMemo`）：
渲染开始时各扫一遍建索引，之后每次查 O(1)，渲染一结束立刻丢掉。

> 关键是**生命周期只限一次同步渲染**，不做长期缓存。
> 发送会把 outbox 条目的 status 就地改成"已发送"，数组引用和长度都不变，
> 任何「按引用+长度」的失效判据都会失灵，质量分会一直停在发送前的旧值。
> 不在渲染中时索引为 null，各处自动退回原来的扫表写法，行为完全一致。

**第二类：DOM 节点爆炸。** 这类优化 JS 一点用没有。潜客页 2000 条线索会生成
**3.4 万个 DOM 节点、2.2MB HTML**，其中拼字符串 164ms、写 innerHTML 51ms，
而浏览器**强制排版 540ms**。修法是不生成这么多节点：

- 潜客表每次渲染 200 行，其余「再显示 200 条 / 全部展开」
- CRM 看板每列封顶 60 张卡，列头的数量和金额仍按全部算
- 发信队列每页 200 封（勾选在 state 里，所以没渲染出来的照样能被全选和批量发送）
- 收件箱会话列表每页 150 条

> 分页最容易踩的坑是**静默改掉「全选」的口径**。原本 `visibleProspectIds()` 是数
> DOM 里有几个复选框，一分页就会从「全选当前筛选结果」悄悄变成「全选当前页」，
> 而界面文案一个字没变。现在完整的筛选结果 id 记在 `mkdFilteredProspectIds` 上，
> `bench-scale.mjs` 里有专门的守卫盯着这一条（改坏了退出码 1，已验证过它真的会失败）。
> 队列同理：守卫会全选一遍，确认批量发送解析出的条目**包含没渲染出来的那些**，
> 并且会拒绝在"队列不足一页"的数据上跑——那样比对是空的，绿灯没有意义。

**发信队列的勾选搬进了 state**（`mkdSelectedOutbox`），顺带修掉一个原先没人注意的 bug：
勾选只存在 DOM 复选框上，而 `renderOutbox` 重建 innerHTML 时复选框是全新的、没有 `checked`
——所以**任何一次重渲都会静默清空已勾选的邮件**。用户勾好十几封，后台自动驾驶跑一轮触发重渲，
勾选就没了，界面上没有任何提示。搬进 state 后勾选跨重渲存活，队列也就能整体分页了
（每页 200 封，2000 条时 232 → 78ms，节点数 22080 → 6695）。

收件箱的会话列表同样封顶（每页 150 条，5000 条时 459 → 41ms）。它是单选，不涉及批量口径。

顺带修正的一处文案：「批准并发送（N）」里的 N 原本是**预检通过数**，但这个按钮只发**勾选的**，
两个数经常对不上。现在显示「已选 N」，没勾选时按钮禁用并提示「先勾选」。

**走查补记（2026-08-22 当天复查）**：上面那套索引一开始只挂在 `render()` 上，
但**筛选框、排序、分页展开、收件箱操作、分析页时间范围**这些交互是直接按名字调
`renderProspects()` / `renderOutbox()` / `renderAnalytics()` 的，不经过 `render()`——共 14 处。
那些路径没有索引，就退回逐条扫全表，于是"改完还是卡"，而且**卡在用户最常点的地方**：
2000 条线索时，分析页切一下时间范围要 **1.16 秒**。

修法不是补那 14 个调用点（下次新增一个还会漏），而是把作用域放进函数自己：
在 `09-netprobe.js` 末尾给 `renderProspects/renderOutbox/renderInbox/renderCrm/renderAnalytics`
各套一层 `withScanIndex`（可重入，嵌在 `render()` 里不额外花钱；`VIEW_RENDERERS`
在加载时抓的是原始引用，所以 `render()` 内部不受影响）。

直接调用耗时：分析页 1161 → 46ms，队列 204 → 10ms，潜客页 52 → 7ms。

同时发现两处小问题并修掉：`deriveDealStage` 仍在逐条扫 `state.inbound`
（导入一批新线索后首次进 CRM 就是 O(线索 × 来信)）；`renderOutbox` 的空队列早退分支
没清 `mkdSelectedOutbox`，换活动后「已选 N」会显示一个界面上根本没有的数字。

> 另外把 `bench-scale.mjs` 的增长判定接上退出码——原本判出「平方级 ✗」也只是打印一行，
> 发版检查照样通过。现在 ≥15× 阻断发版（8～15× 只告警，机器负载和 GC 抖动够得到 8，
> 够不到 15）。左边那一列量的就是不带索引的直接调用路径。

**分析页 5000 条 389ms**：调用次数已全部是线性的（实测 ×4 数据 → ×4 调用），
剩下的是分配/GC 的量级效应，不是算法问题。1000~2000 条时只有 23~67ms，真实规模下不构成问题。

### 第四轮（2026-08-22）：模块级状态的重置路径

分页和勾选引入了 8 个模块级变量（`mkdProspectShown` / `mkdOutboxShown` /
`mkdConversationShown` / `mkdSelectedOutbox` / …）。这类状态**不跟着 `state` 走**，
所以"该重置时没重置"是它特有的坑，静态走查工具查不出来。

**抓到一个严重的：「全部展开」是永久状态。** 它原本把 `mkdProspectShown` 设成
`Number.MAX_SAFE_INTEGER`——点一次之后这个状态就黏住了，往后每次渲染都把整池铺进 DOM。
实测：700 条时点一下展开，之后导入 5000 条，一次渲染 **2150ms、8.5 万个 DOM 节点**。
分页保护被一次点击悄悄废掉，而且用户既看不出来、也没法撤销（只能靠改筛选条件）。

修法分两层：

1. **结构上去掉"无限"这个状态**——「全部展开」= 展开到*当前这批结果*，
   新进来的数据重新出现「显示更多」。修后同样场景 309ms、1.2 万个节点。
2. `resetListPaging()` 挂在三个会整批换数据的入口上：换活动 / 恢复备份 / 重置演示数据。

守卫写在 `bench-scale.mjs` 里：展开到 700 后把数据涨到 5000，渲染行数必须仍是 700。
（退回无限展开后该项报 5000 行、退出码 1，验证过。）

> **浏览器验证前先核对构建戳。** 这一轮两次被 `index.html` 的浏览器缓存坑到：
> 页面加载的是旧 `app.js?v=...`，导致"改了没生效"的假象，差点去改一段本来正确的代码。
> `python -m http.server` 不发 no-cache 头。用
> `http://localhost:4174/index.html?bust=<随便什么>` 加载，并核对
> `document.scripts[0].src` 里的 `v=` 和磁盘上 `index.html` 一致。

### 第五轮（2026-08-22）：错误路径 —— 东西坏掉的时候会说什么

前面所有测试测的都是"好用的时候对不对"。这一轮测**坏掉的时候**，判据只有一条：

> **失败绝不能被伪装成结论。**

抓到两个，都是同一个形状：**「没查成」被报成了「查了，是否定结果」**。

**① 合规筛查（高危）。** 主进程那边写得很清楚——名单读不出来就返回 `ok:false`，
注释还专门写了"绝不能静默返回没命中"。但**批量筛查传 `quiet=true` 把失败日志压掉了**，
循环跑完照常报：

```
合规筛查完成：查了 3 家，精确命中 0 家、疑似 0 家
```

零失败提示。用户读到"查了 3 家、0 命中"就发货。触发条件很现实：
`package.json` 的 `build.files` 漏掉 `data/` 在这个项目**真实发生过**。
修法是让 `screenProspect` 失败时返回 `{ok:false}` 而不是 `null`
（返回 null 的话，"没查成"和"查了没命中"在调用方眼里长得一模一样），
批量循环单独统计并在汇总里明说。

**② 官网抓取（中危）。** `harvestProspectSite` 在**打不开网站**时返回 `"none"`，
和"打开了但没公示联系方式"是同一个值，于是汇总会说"这批公司官网上既没有公示邮箱，
也没有号码"——用户据此把好线索删掉。改成返回 `"unreachable"` 并单独统计。
（讽刺的是，SPA 那段注释里已经把这个担心写过一遍了，只是没覆盖到"打不开"这种情况。）

**新增 `tools/test-error-paths.mjs`（18 项）**，覆盖五类失败面：
数据集损坏 / IPC 失败传递 / 批量汇总口径 / 外部输入损坏（AI 乱码、备份损坏、存档花掉）/ 打包完整性。
每一条都验证过"bug 存在时会红"。

> **这一轮自己踩的坑值得记：第一版的数据集测试是完全空转的。**
> 那些 electron 模块写的是 `require("node:fs")`，我的桩只认 `"fs"`，
> 于是 fs/zlib/path/dataset-age 全部拿到空对象，模块每次都因为"桩缺东西"抛异常——
> 测试一直是绿的，但**测的是我的桩，不是产品代码**。
> 后来给 `load()` 故意加了个"空名单兜底"来验证，测试竟然还是绿的，才发现。
>
> 现在只假造 `fs`，其余全用真模块，并且加了一条**反空转自检**：
> 喂真实的 `data/screening.json.gz` 必须能跑出 40030 条名单，跑不出就说明
> 这套加载方式本身是坏的，上面那些"抛错了 ✓"一条都不算数。
> 这个模式值得复用到别的桩测试上。

顺带把 `bench-scale.mjs` 和这个套件共用的 DOM 桩抽成了 `tools/app-sandbox.mjs`
——两份各自维护的桩一定会漂移，表现是其中一个莫名加载失败，还很难判断是桩的问题
还是产品的问题。

### 第六轮（2026-08-22）：并发与中断

三个批量任务（合规筛查 / 官网抓取 / 邮箱探测）都靠 `if (!runIsActive()) break` 判断
要不要继续，而 `runTracker` 是**单个全局变量**。由此出来两个 bug，都还是那个形状：
**汇总按目标条数报，不按真正跑完的条数报。**

**① 状态条上的 × 是个隐形的取消键。** 它调 `runDismiss()` 把状态置为 idle，
于是循环下一轮就 break 了——但它长得只像"关掉这条提示"。实测：跑到第 3 家时点一下，
批量静默停住，日志照样说

```
合规筛查完成：查了 10 家，精确命中 0 家、疑似 0 家
```

7 家根本没查过，用户被告知全查过了。对合规功能来说这跟没查一样危险。

修法不是把取消功能砍掉（批量能跑几分钟，取消是有用的），而是**把它变诚实**：
跑着的时候 × 的 `title` 变成「中止这个任务」，点它走 `runCancel()`，
会记一条日志、状态条显示「已中止」，汇总按真正跑完的条数说
（"10 条里只跑完 3 条，剩下 7 条**没有执行**"）。已完成的部分保留。

**② 没有互斥，第二个批量任务会掐断第一个。** 第二个 `runBegin` 覆盖 `runTracker`，
第一个下一轮就 break。实测：筛查跑到第 2 家时点「抓官网」，筛查停了但仍报"查了 10 家"。
加了 `runBusy()`：一个在跑时第二个直接拒绝，并说明原因。

**③ 跑到一半换活动**（互斥挡不住，切活动不是批量任务）——查下来是安全的：
写回时走的是 `state.prospects.map()` **重新读当前 state**，id 对不上就什么都不写，
不会把上一批的结果写到新活动的线索上。已固化成测试。

`test-error-paths.mjs` 加了这三条，共 21 项。前两条都验证过"退回修复后会红"。

> 这一轮又栽在自己的桩上两次：`firstChild` 没实现导致 `showToast` 抛错；
> 补上之后 `remove()` 是空实现，`while (children.length > 3) firstChild.remove()`
> **死循环**，命令跑到超时。共享沙箱 `tools/app-sandbox.mjs` 里都补好了。
> 教训还是那条：**桩的问题会伪装成产品的问题**，加桩时要顺手想一想
> "产品代码会不会依赖它的副作用"。

### 第七轮（2026-08-22）：模拟数据会"转正"

顺着前两轮那条主线（失败/伪造被当成事实）往发信侧查。发送失败的处理本身是对的
——SMTP 逐封回执、只有 `r.ok` 才标已发送、失败的保留待发。问题在**本地模拟**这一侧。

本地模式（默认）发信是模拟的，`delivered` 和 `opened` 按哈希算出来。产品是知道的：
日志写了"本地模拟发送"，A/B 主题面板也警告了"打开数是假数据"。但有两个洞：

**① 分析页顶部的 KPI 卡和主漏斗完全没提示。** 那里有「打开率 88%」和标着
「北极星指标」的有效询盘——全是编的，一个字的说明都没有。而下面那个小得多的
A/B 面板反而警告了。用户最先看的是 KPI 卡。

**② 更隐蔽：假数据会随着切换模式"转正"。** 所有提示判的都是
`state.settings.mode === "local"`。用户先用本地模式跑一周（50 封假的送达/打开），
之后配好 SMTP 切到直连——`mode` 变成 `"direct"`，**所有提示消失**，
而历史里那 50 封仍然混在漏斗和北极星指标里，再也分不出来。

修法是把标记落在**条目上**而不是靠全局模式现判：
`deliverEmail` / 本地派发 / WhatsApp 本地队列写 `simulated: true`，
`markEmailSent`（SMTP 与 Webhook 两条真实路径都走它）写 `simulated: false`。
分析页按**实际条数**提示：

- 全是模拟 → "送达和打开全部是程序生成的假数据"
- 真假混合 → "已发出的 6 条里有 3 条是本地模拟的（另有 3 条是真发的），两者混在一起算"
- 全是真发 → 不提示（别喊狼来了）

A/B 面板的判据也换成同一个信号。老数据没有标记，用 `!item.messageId` 兜底——
判不准时宁可多提示。

`test-error-paths.mjs` 加了这四条（共 25 项），都验证过退回修复会红。

> 顺带修了个排版 bug：`analytics-kpis` 是 5 列 grid，提示条不跨列就被当成一张卡片，
> 挤成 190px 宽、文字竖着堆到 137px 高。加 `grid-column: 1 / -1` 后占满整行、高 39px。
> **这类问题只有真机能看出来**——DOM 桩里 innerHTML 是字符串，测试全绿也说明不了排版对。

## 四、主要模块在哪

| 文件 | 作用 |
|---|---|
| `electron/netprobe.js` | 抓官网、DNS 体检、SMTP RCPT 探测。零第三方依赖 |
| `electron/screening.js` | 合规筛查：OFAC / UFLPA / BIS 名单归一化匹配 |
| `electron/hscode.js` | HS 六位目录查询与错码分型 |
| `electron/tenders.js` | 公共部门货物采购官检索 |
| `electron/customs.js` | 本地海关提单库（只查不导） |
| `src/09-netprobe.js` | 上面这些的渲染层：护航面板、首封向导、行为分支、定位校准、筛查/HS/采购官 UI |
| `data/*.json.gz` | 三份内置数据集，合计 1.2 MB，随包发货（`package.json` 的 `build.files` 必须含 `data/**/*`） |

### 值得记住的坑（都已修掉并写进测试，别踩回去）

1. **国内 DNS 返回残缺 TXT**。查 github.com：系统默认 8 条无 SPF、8.8.8.8 22 条有
   SPF、阿里 223.5.5.5 17 条无 SPF。据此让已配 SPF 的用户"再加一条"，两条 SPF 按
   RFC 7208 直接失效——**会亲手搞坏用户的送达率**。现在多解析器取并集 + 查不准就说查不准。

2. **没配追踪时 `opens` 恒为 0**，`cold` 分支会把所有人判成"没打开"并自动暂停全部序列。
   **测不到 ≠ 没打开**。

3. **定位校准会误伤用户自己的产品词**。卖无人机配件的人标记一家无人机工厂为不对口，
   `drone` 进排除词就等于废掉他的全部搜索。现在有 `protectedTerms()` 保护。

4. **合规筛查剥词要有分寸**。剥 `CO/LTD/GmbH` 是对的（让"少个逗号"不影响比对），
   但**不能剥** `TRADING/HOLDING/INTERNATIONAL`——那样 `ACME TRADING` 和
   `ACME HOLDINGS` 会一起压成 `ACME` 双双误报。误报和漏报一样有害。

5. **`noticedate` 是 `DD-Mon-YYYY` 文本**，按字符串比大小会算错
   （`'04-Aug-2026' < '31-Oct-2025'`）。我用它得出过一次错误结论。

6. **JS 模板字符串里的 `` 是退格符不是词边界**。`new RegExp(\`\b(...)\`)` 少一个
   反斜杠，正则里就没有词边界，规则**静默失效**（不报错、看着对、就是不生效）。
   合规筛查的剥词最后改成了 token 过滤，绕开这类转义坑。

## 五、当前分支状态

- 主线：`main`，且**只有这一条分支**。2026-08-06 远程历史被有意重写过一次
  （37 条压成 7 条，根提交换成 `722a660`），旧分支与本地备份分支都已删除。
  下次 `git pull` 如果又提示不能快进，先 `git ls-remote origin` 确认是不是又重写了，
  再决定 `reset --hard`——**动手前务必先核对新历史里有没有你的东西**。
- 工作区干净，16 个套件 223 项全绿
- 最新提交见 `git log -1`；本轮改动的完整说明写在 `CHANGELOG.md` 顶部那一条

> ⚠️ **这个仓库永远不要跑 `git clean -fdx`**：`tools/keys/private.pem` 是未跟踪文件，
> 会被一起删掉，而它是全世界唯一能签出激活码的那份。`node_modules` 也会一起没。

### 换机后先确认三件事

1. **构建可复现**：`node build.mjs` 之后 `git status` 应当是空的。
   如果 `app.js` / `index.html` 变脏，说明版本戳又对不上了（历史上栽过两次，
   都是有输入没做行尾归一化）。
2. **不是试用版**：双击「觅客舵（开发模式·VIP）」启动，窗口标题里**不该有**
   「（试用版）」。试用版会把 `aiEnabled()` 硬改成恒 false，AI 那半条链根本
   跑不起来，会误以为是业务逻辑坏了。
3. **AI 引擎能连通**：设置 → AI 引擎 → 测试连接。失败时看「自动化日志」第一行，
   它会写明请求实际发往哪个域名、Key 的首尾 4 位与长度、以及是不是这家的
   Key 格式——三样凑齐基本能一次定位。

### 两个还没查的可疑点

- 08 的「上手五步」和 09 的「发出第一封信」都往控制台首页塞，可能同时渲染
  两个引导面板。开首页一眼就能看出来。
- `aiEnabled()` 被 08 用 `__mkdBaseAiEnabled` 包了一层试用限制。项目里这种
  "后面模块包装前面函数"有好几处，包装顺序错了会静默失效，值得统一梳理一遍。
