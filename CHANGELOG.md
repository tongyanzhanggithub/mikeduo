# 更新日志

按主题记录本轮对系统的优化与新功能。运行方式不变（双击桌面图标 / `npm start`），源码改为 `src/` 模块化 + `node build.mjs` 构建（详见 `src/README.md`）。

## 2026-08-19 · 接入一份公开贸易数据集：能用的抽出来，不能用的说清楚

拿到一份 395 MB 的外贸数据集（16 张表/视图：秘鲁贸易、世行采购、OFAC/UFLPA/BIS
制裁名单、HS 目录、美国关税与裁定等）。先做了一轮数据勘察，逐项判能不能用，
再决定接哪些——**没有一股脑全接**。

结论是三块能用、一块明确不做：

| 表/视图 | 判定 | 落地 |
|---|---|---|
| `v_screening` 40,595 行 | 🟢 直接可用 | 合规筛查接进发信闸门 |
| `hs_codes` 8,261 条 | 🟢 可用 | 校验 AI 报出来的 HS 码 |
| `v_tenders` / `wb_procurement` | 🟡 换个用法才可用 | 做成采购官联系人库，不做标讯 |
| `v_buyers` 216,307 行 | 🔴 不做 | 只有秘鲁一国、无产品维度，覆盖不到目标市场 |

395 MB 原库不可能随包发货，但**只抽真正要用的字段，三份加起来 gzip 只有 1.2 MB**，
全部内置、全程不联网。

### 🛡 合规筛查：发信前查对方在不在制裁名单上（452 KB）

这是产品里一直空着的一块，而它和「护航」那条叙事完全同向：我们拦的不只是退信，
也包括法律风险。名单 40,030 个主体（OFAC + 别名 + UFLPA + BIS-DPL）。

数据方点名的三个坑，逐条处理并写进测试：

**① 绝不能用 `=` 精确匹配。** `CHINA SPACESAT CO., LTD.` 是名单主体，写成
`CO. LTD.`（少一个逗号）精确匹配就失效——**漏判有法律后果**。改为归一化后比对：
转大写、去标点、去 `CO/LTD/GmbH/FZE/有限公司` 这类法律形式后缀。实测三种写法
（含全小写）都能命中同一条。

> 但刻意**不剥** `TRADING / INTERNATIONAL / GROUP / HOLDING / IMPORT / EXPORT`
> ——那些是名字的组成部分，剥掉会把 `ACME TRADING` 和 `ACME HOLDINGS` 一起压成
> `ACME`，双双撞上名单里的 `ACME`。**误报和漏报一样有害**：用户会不敢做正常生意，
> 然后不再信任这个功能。实测 5 家正常客户零误伤。

**② `CMIC-EO13959` 不是贸易禁令。** 这一类只限制美国人买卖其证券（中国移动、
中海油都在内）。标成「禁止交易」是明确误报。现在单独识别，判为 info：提示但不拦。

**③ 三条法律线不可合并成「被制裁」。** OFAC 是资金走美国渠道时受限；UFLPA 是
货到美国口岸即被扣、举证责任在你方；BIS-DPL 是供含美国成分货物即违规。命中哪条，
后果完全不同，分别给出说明。

判定分级：精确命中 block 级 → 拦下发送；证券限制类 → 只提示；名称相近 →
只警告不阻断。误报一定会发生，所以给了留痕的人工推翻入口——必须填理由，
导出可见，责任归属写在确认框里。

实现上两个决定：结果落在线索上（照 `emailProbe` 的做法），`preflightOutboxItem`
只读不查（它是同步的、会被队列每一行反复调用，不能在里面走 IPC）；名单读取失败时
**明确报错，绝不静默返回「没命中」**——那会让用户以为查过了，实际根本没查。

### 🔢 HS 编码校验：AI 报的码第一次被验过（158 KB）

「AI 细化定位」会产出一个 HS 编码写进活动配置，但**从来没人校验过它是否存在**。
模型报一个不存在的码，用户拿去查海关数据、填报关单、跟客户对话，一路错到底，
而且没有任何一环会告诉他错了——这和我们花大力气清掉的「编造联系人」是同一类问题：
**把模型的输出当事实用**。

判定不是简单的「有/无」，按模型实际会犯的错分型：

| 模型给的 | 判定 | 界面给什么 |
|---|---|---|
| `847989` / `8479.89` / `8479 89` | 有效 | 摊开 `84 → 8479 → 847989` 三级层级 |
| `84` / `8479` | 有效但**不够细** | 报关要用到六位子目 |
| `847999` | 查不到 | **定位到最近有效上级 `8479`**，列出同级可选码，点一下就改 |
| `8479891000` | 不是错码 | 各国在六位后自行扩展（中国 8 位、美国 10 位） |

后两行是关键：模型最常见的错法就是「前几位对、后几位编的」，只说一句"无效"
用户不知道该改成什么。

顺带发现 HS 用的是法条式措辞，和日常叫法常常对不上——**搜 `drone` 一条都没有，
它写作 `unmanned aircraft`**。搜索框在搜不到时会直接把这件事讲出来。

只留 `revision='HS'`：实测 H6 是它的子集（H6 独有 0 条、交集里描述完全一致），
两套都带只会让同一个码出现两遍。

### 🏛 公共部门货物采购官：4,433 位真实联系人（515 KB）

`wb_procurement` 有 40 万条世行融资项目采购公告，带联系邮箱的 10.6 万条，
邮箱填充率 100%、姓名电话 99%。

**但直接当"招标机会"卖是骗人的**，实测两条：GO（货物）类公告的
`submission_deadline_date` 全部为空；全库截止日未过的只有 231 条。
所以做成联系人库，界面第一句就写「这是联系人库，不是招标机会」。

> 这里我自己先算错过一次。`noticedate` 是 `DD-Mon-YYYY` 文本，按字符串比大小
> 会得出"数据截至 2025-10-31"——因为 `'04-Aug-2026' < '31-Oct-2025'`。
> 正确解析后 **2026 年仍有 1,584 条货物采购公告**，数据是当年的。
> 结论因此修正为：**公告过期了，人还在**。

主筛选维度做成「只看 2025 年后仍在发采购公告的」——1,299 位今年还在采购的人。

只留 GO（货物）17,316 条 → 4,433 位去重采购官。CS（咨询服务）46,526 条、
CW（土建）16,083 条对卖货的人没有意义，不收。

**刻意不混进主线索池**，单独挂在线索发现页：画像完全不同——发展中国家公共部门与
国际组织、品类杂、单子偏小、走公开采购流程。混进去用户会拿给进口商写的开发信模板
去发采购官，口径完全不对。导入时日志里也会把这句说清楚。

数据方点名的坑照原样传给用户：官方公布的联系邮箱约 **44% 是 gmail 之类的个人
免费邮箱**，**不要按「个人邮箱=不可信」过滤**，那会杀掉大量真实机构联系人。
代码里没做这种过滤，并专门写了一条测试防止以后有人"顺手优化"掉。

### 🧩 这类数据的通用接入原则

这份数据集最值得抄的地方，是它自带 `data_caveats`（29 条）与 `data_notes`（30 条），
并要求"展示查询结果时必须一并展示限制说明"。三个内置数据集都照这个做了——
每份都带 `builtAt` / `dataThrough` / `caveats`，界面上跟着结果一起显示。

两条写死的规则：

- **鲜度分层**：名录类（公司、联系人、HS 码、制裁名单）老化慢，机会类（招标、
  截止日）老化极快。`v_tenders` 就是活证据——当机会看只剩 231 条，当名录看
  17,233 条全能用。机会类必须显示"数据截至 X 日"。
- **逐行可溯源**：每条派生线索带来源标识，和官网抓取的 `sourceUrl` 是同一个原则
  ——**不能溯源的联系方式等于没有**。

### 🔧 开发体验：改了源码但窗口还是旧的

实际踩过一次：改完 `src`、跑了 build，但开着的窗口还是旧的——它在启动那一刻就把
`app.js` 读进内存了。`index.html` 里的缓存哨兵抓不到这种情况（页面和脚本同时加载，
二者自洽，哨兵只能发现"页面新脚本旧"）。

现在窗口重新获得焦点时比对磁盘上的真实版本戳，不一致就浮一条提示带「刷新生效」。
打包版恒返回 null，装机用户看不到。

> 第一版是坏的，靠实测抓出来：只读 `app.js` 前 4096 字节找版本戳，但戳实际在
> ~10.4KB 处，于是恒返回 null——代码看着对、一行不报错、就是永远不提示。

另外补上试用版启动器：上一轮「只留一个启动器」把普通启动器删了，但开发模式脚本的
注释里还指引「用普通启动器看试用版长什么样」，那个文件已经不存在。现在桌面两个图标：
**觅客舵（开发·VIP）** 与 **觅客舵（试用版）**。

### ✅ 验收

自动化测试 130 → **177 项**，14 个套件全绿。新增四个：

| 套件 | 项数 | 覆盖 |
|---|---|---|
| `test-screening.mjs` 🆕 | 15 | 归一化匹配、CMIC 不是禁令、三条线分别说后果、正常客户零误伤 |
| `test-tenders.mjs` 🆕 | 13 | **前四条测的不是功能是诚实性**：必须否认自己是标讯、必须给数据截止日 |
| `test-hscode.mjs` 🆕 | 11 | 模型各种写法、层级、"前几位对后几位编的"要定位到有效上级 |
| `test-build-stamp.mjs` 🆕 | 8 | 版本戳不在前 4096 字节，main.js 不得截断读取 |

---

## 2026-08-15 ~ 08-18 · 把"找到公司"改成"找到买家"，并第一次真的打了个包

这一轮的起点是一句话：**系统找的是域名，不是买家。**

从搜索/AI/粘贴进池的线索，以前只过三道筛子——域名不是平台站、不在退订名单、
没重复。市场研究报告站、同行工厂、行业媒体、招投标平台，跟真正的进口商是以
完全相同的身份进池的。而唯一判断"这家到底买不买我的东西"的 fit_score，
只在「官网深挖联系人」里产生，那是一条线索一次联网调用——**最贵的筛子装在了
最后一环**：等你花钱把整池挖了一遍，才知道其中一半根本不是客户。

### 🚪 入池体检：筛子挪到入口，并且改成批量

挂在 `admitProspects` 这个唯一闸门上，三条入池路径一次覆盖：

1. **抓官网**（免费、不要 key、抓 2 页）。顺手把企业公示的邮箱和 WhatsApp 号
   落进线索，同时把官网自述留作判断材料。
2. **一次 AI 调用判一批**（20 家）。依据是这家公司自己官网上写的话，不是
   Google 摘要那一行。判不对口的走既有 offTarget 通道，自动被批量入队和
   自动驾驶跳过。

成本方向是反的：以前每条一次调用，现在每 20 条一次。

判死的口径特意从严——**错判一家真公司出局，比放进来一家假的代价大得多**：
只有两次尝试都拿到 ENOTFOUND 才算域名不存在；超时、403、拦爬虫、证书问题
一律不判死；robots.txt 整站封禁时请求压根没发出去，更不算域名的错。

界面上死站和不对口分开措辞：域名不存在是事实，不是 AI 的判断，混着说会让人
以为可以推翻它——而假域名推翻不了，往那儿发信只会退信。

### 🔓 两个功能不再锁死在 Claude 上

「竞品渠道反查」和「官网深挖联系人」以前写死了只能走 Anthropic 的服务端
联网工具，用 DeepSeek 等模型的人根本用不了。但这两件事要的其实只是
**"读一个已知网址下的页面"，不是"上网搜索"**——桌面版自己就能把页面抓回来，
模型只需要读。

- 默认走本地抓取 + `callAI`，任何服务商都能用；抓不到时才退回 Claude 联网
- 抓回来的比让模型自己联网**信息更多**：正文之外还把页面上所有站外链接连同
  锚文本一起给它——经销商页上那些外链本身就是经销商的官网域名
- 解析用 `DOMParser` 而不是正则：真实网页 HTML 常常不规范（标签没闭合、
  属性值里带 `>`），正则一碰就散
- 给每个块级元素补换行再取文本，否则
  `<h3>Nordwind Agrar GmbH</h3><p>Hamburg` 会读成 `GmbHHamburg`，
  模型据此抽出来的公司名是错的

现在还依赖 Claude 联网搜索的只剩「联网找客户」与「公司名→域名解析」，
前者可改配 SerpAPI 直连。

### ✍️ 首封信第一句：我为什么找到你

原来所有品类模板的开场都是 `We understand X may source Y for the Z market`。
这句对谁都成立，也就等于什么都没说，正是群发的招牌——一个欧洲分销商的
`info@` 每天躺着几十封长得一模一样的中国供应商开发信。

新的开场按证据强度分档：提单+现供应商 / 只有提单 / 只知现供应商 /
竞品经销商页 / 官网公示。有硬开场时，那句 `may source` 的对冲话整段剔除
（刚说完"公开提单显示你进了 12 次货"又说"我们了解你可能采购"，自己打自己脸）。

**拿不出硬事实时返回空串，让信回到原来的中性开场。** 这里刻意用结构化字段
拼模板而不是让模型写：模型看到"写个有说服力的开场"一定会编，编一句
`I noticed your recent expansion in the German market`，对方一眼识破，
比群发更糟。拼模板只可能说出我们真的知道的东西。

配套：海关线索不再丢掉供应商名字。以前算出来了却只把数量写进采购信号
（"现有 3 家供应商"），而**名字才是这条线索上最值钱的事实**。

### 📊 获客来源效果：哪条搜索式在白跑

每条线索一直记着它是从哪条搜索式、哪个渠道来的，但从来没人把它和"后来
回没回信"对起来。新面板按回复数排序，全部本地计算，零接口成本。

口径写得保守：冷开发信回复率本就是个位数，发 5 封没人回是纯噪音。
**触达满 10 家才给回复率**，不足的写「样本不足」；**到 20 家还挂零才标「白跑」**。

分组有个坑：`searchQuery` 在不同来源下含义不一样——SerpAPI 存的是真正的
搜索表达式（多条线索共用），Claude 联网和粘贴导入存的是每条线索各自的说明。
所以只有被两条以上线索共用的字符串才当搜索式单独成行，其余按渠道归并。

### 📦 第一次真的打了个包，并堵上一个安装通道口子

`npm run dist` 从来没跑过。这次跑通了（安装包约 80MB），也确实炸出了东西。

**Windows 上第一次打包必然失败**：electron-builder 解压 winCodeSign 时要建
macOS 用的软链接，普通用户没这个权限。两种解法写在 `docs/交接与待办.md` 第 10 条。

依赖单独验了一遍——`mailer.js` 是在函数体内才 require nodemailer/imapflow 的，
**应用能启动根本证明不了它们能加载**，真等用户点「测试连接」才炸就晚了。
结果：都能从 asar 里 require，全包没有原生模块，托盘图标能读出来。

炸出来的问题是 `publish.owner` 还是 `REPLACE_ME`，装完启动就打一次
`github.com/REPLACE_ME/mikeduo-release` → 404。两层危害，第二层才要命：
**REPLACE_ME 是谁都能注册的 GitHub 账号名，一旦被抢注，它立刻成为所有已装
机器的更新源**——等于把安装通道拱手让人。加了闸门（`electron/update-feed.js`）：
owner 是占位符、为空、或读不到文件，一律不发这个请求。

### 🧹 顺手

- 清掉废代码：一个被同名函数覆盖、永不执行的 56 行函数，两处死定义，
  42 条匹配不到任何元素的样式
- 管理页加「全部活动总览」：跨活动汇总，汇总用分子分母各自相加而不是把
  各活动的比率平均（否则发了 2 封的小活动和发了 200 封的大活动等权）
- 修一个藏了很久的 bug：`looksLikeQuery` 正则里两个 `\b` 是**真的退格符
  0x08**，那个分支永远匹配不上——语法合法、测试全绿、肉眼看不出来。
  新增 `tools/test-source-hygiene.mjs` 扫全部源码的控制字符
- 两处 CSS 覆盖：文件末尾改版区的 `.tag` 同权重排在后面，把「白跑」徽章
  （对比度 1.04，等于隐形）和「当前」徽章（渲染成灰色）的配色都盖掉了
- `npm test` 从 2 个套件补到 5 个（31 → 69 项）

## 2026-08-06 · 接上第三方中转，以及一轮"用户点了但不知道发生了什么"的清算

这一轮全部由真实使用暴露出来。起点是一个反复出现的现象：**用户照着提示做了，
软件却什么都不说，或者说的是错的**。往下挖，发现同一个病根有好几种长相——
两处代码各算一套数、陈旧字段永久压过当前选择、报错文案指向错误的原因。

### 🔌 第三方中转：三处写死的地址

「云端大模型」配好中转站 Key 一直报 `invalid x-api-key`。原因是
`callAnthropic()` 和 `callClaudeWebSearch()` 把 `https://api.anthropic.com`
写死了，**配置里的 Base URL 从未生效**——中转站的 Key 被原样发去了官方接口。

- 中转地址对所有服务商开放（原来只有「自定义」可填）
- 只填域名会自动补全路径，中转站文档里三种写法都能用
- 填了中转地址时额外带 `Authorization: Bearer`（中转站多只认这个）；
  官方接口同时收到两种凭证会被拒，所以不能无条件加
- 经中转时 `web_search` 退回基础版工具，避免中转站未跟进新版本导致整条链路 400

### 🧭 服务商选了不生效：一个陈旧字段压了所有选择

比中转更隐蔽。`aiProviderId()` 里有一条旧存档兼容：

    if (state.settings.aiEngine === "claude") return "anthropic";

它不是一次性迁移，而是**每次读取都重新解释**。只要存档里留着这个遗留值：
下拉选通义/DeepSeek，`aiProvider` 确实存进去了，但请求永远发往
`api.anthropic.com`；而每次 render 又用它回写下拉，**选择当场被弹回 Claude，
用户在界面上根本逃不出这个状态**。

改成读档时一次性迁移：引擎归为 `cloud`，服务商保留用户存过的选择
（没存过才落到 anthropic）。实测存过 qwen 的存档迁移后下拉与接口一致，
再改选也不会被弹回。

### 🧩 服务商与模型

- 服务商从 7 家扩到 16 家，新增豆包（火山方舟）、腾讯混元、文心一言、
  讯飞星火、MiniMax、阶跃星辰、零一万物、百川、硅基流动，按国产/海外分组
- 各家接口地址不可能全部记准，所以每家都走「可改中转地址 + 拉取可用模型」
  这条路：地址填错能自己修，模型清单直接问服务商要
- 模型选择器从 `input+datalist` 换成 `select`。datalist 会按已填内容过滤，
  字段里预填了模型名时下拉**只剩那一条**，看起来像"只有一个模型"
- 新增「拉取可用模型」：调 `GET <版本段>/models`。按尾巴替换路径不够用
  （MiniMax 是 `/v1/text/chatcompletion_v2`），改成截到版本段再接 `/models`
- 拉取成功后若当前模型不在清单里，自动换成清单里的第一个并说明——
  否则刚拿到权威清单，一个确定用不了的模型还占着选中位，下一次调用必然失败

### 🩺 报错必须指向真正的原因

同一句"失败"背后有五种原因，之前都报同一句话。现在逐一分开：

- **Key 形状**：密码框一串点，看不出换没换。报错里带首尾各 4 位和总长度
  （中间永不显示），并按服务商比对前缀——不是 `sk-` 开头就直说"多半不是这家的"
- **请求地址**：日志写明请求实际发往哪个域名，服务商选没选对一眼可辨
- **协议错配**：同一个网关常给两个 Base URL（OpenAI 兼容 `/compatible-mode/v1`
  与 Anthropic 兼容 `/apps/anthropic`），挑错必然 404 而 404 本身看不出原因。
  现在直接说明并给出两条修法
- **模型不存在**：列出已拉到的可选模型，而不是笼统说"名字不对"
- **一键起量归零**：联网找客户有四种归零方式（服务商不支持 / 接口报错 /
  解析不出公司 / 全被去重），之前一律提示"请配置 Claude"——用户明明已经配好了。
  现在四个分支各自记录原因与补救入口

### 📊 运行状态条：长任务不再是黑箱

点「准备获客队列」「解析任务」后界面毫无反应，几秒后又被切到别的视图，
无从判断任务开始没有、在跑、跑完了还是中断了。

- 顶栏下方新增状态条，独立于视图，四种状态各有图标+颜色+文案：
  进行中（当前步骤 + 已用时，每秒刷新）/ 已完成（结果摘要 + 下一步按钮）/
  失败（原因，不自动消失）/ 已中止（缺什么 + 去补齐的入口）
- 前置条件不满足时直接 return 的路径原来只是页面一跳、不给任何解释，
  最像"点了没反应"的正是这条，现在也会说明
- 跑通但一条都没入队时说清是邮箱未验证，而不是干报"完成"
- 两个最主要的按钮补上防重复点击（项目里另外 12 个按钮早就有了，这两个漏了）

### 📥 粘贴导入：整页复制会灌进几十条垃圾

之前建议用户整页复制 Google 结果——实测 27 条里只有 2 家是真公司，
其余全是「跳至主要内容」「全部」「下一页」「Google 帮助」这类界面文字。

- 文本里出现过网址/邮箱时，没有任何联系方式的行一律丢弃（纯公司名清单不受影响）
- `www.` 与非 `www.` 视为同一家
- 面包屑残片（`› patio-furniture`）不再当公司名，有域名就用域名反推
- 平台站过滤补上 tradewheel / tradeford / usimportdata / kompass / indiamart
  等目录站与海关数据商——搜 importer 时它们恰恰排在最前面
- 正则从 `^` 锚定改为 `(^|\.)`：`us.tradeford.com` 这类国家子域原来整批漏过

### 🎯 一键起量接上 SerpAPI 兜底

找客户环节原来只有两条路：Claude 内置联网，或演示数据。**SerpAPI 直连早就
实现了，但这里根本没接**——用户配了也用不上，Claude 联网一断整条流水线就废。

改成三档顺延：Claude 联网 → SerpAPI/搜索 Webhook → 演示数据。
关键取舍：**配了真实来源却失败时不退到演示数据**——悄悄拿假公司顶上，
会让用户以为找到了真客户，比直接报错更糟。

### 🧷 红点与审批：同一批活只标一次

- 徽标与「审批中心」面板原来各算一套，同一批数据徽标算 3、面板显示 4
- 审批中心漏算「已批准待发的 AI 回信草稿」：两条邮件分类没把
  待审批+待发送切干净，`待发送 + reply` 两边都不算
- **红点只标在能动手的页面**：管理页去掉红点（它只做总览），
  WhatsApp 队列本来就和邮件队列同在触达队列页，合并计入该页红点
  （之前它没有任何红点，只能从管理的汇总里间接看到）
- 审批中心有数的卡片排前面——四类里常常只有一类非零，固定顺序会把唯一有
  内容的压到面板底部，用户看到前几张全是 0 就以为这里没东西
- 页面上直说这里是跨页总览、活在哪一页，不再只写在悬停提示里

### 🖱️ 上手五步改成就地可做

每一步原来只有一个「去这里做」，点完跳到另一个页面还得自己找输入框。
现在四步能在卡片里直接做完：填产品、粘贴解析线索、抓官网联系方式、
发一封给自己。发件邮箱那步不做第二套表单（设置页那套有完整的 SMTP/IMAP
和测试连接，复制一份既容易走样，也多一个填授权码的地方），改成跳过去
正好落在「发信」那一段。

### 🎨 界面一致性

- **主题来源统一**：多处组件的深色规则挂在 `@media (prefers-color-scheme)`，
  跟的是系统主题，而全产品其他地方跟 `body.dark`。系统深色+应用浅色时它们
  单独变深且切换应用主题纹丝不动。首封向导、陪跑看板、发信健康度、
  跟进分支徽章、追踪提示全部改为跟随应用主题，`styles.css` 里不再有任何
  `prefers-color-scheme`
- **队列卡片四列错位**：`.outbox-item.selectable` 特异度 0,2,0 盖过了后面那条
  四列规则，选中态按三列排——中间列被压成 0px，复选框溢出去压在标题第一个
  字上，状态列被挤到第二行。实测列定义 `66px 0px 268.98px`，复选框与正文
  重叠 6px；修后 `66px 20px 181.98px 57px`
- 「可发送 0」与按钮「批准并发送（4）」自相矛盾：前者数零提示的，后者数能发的。
  标签改为 无提示/有提示/被拦住，并在队列顶部加一句话说明下一步
- `.btn-ghost` 在样式表里根本没有定义，相关按钮一直用的浏览器默认样式

### 🛠️ 开发与构建

- **开发模式**：试用版把 `aiEnabled()` 硬改成恒 false，AI 那半条链根本跑不起来，
  而发码要私钥、私钥只在签发那台机器上。新增 `MKD_DEV_TIER=basic|pro|coach`，
  只在 `app.isPackaged === false` 时生效——客户装的是打包版，这段代码走不到。
  配套 `启动觅客舵-开发模式.vbs`
- **版本戳跨机器可复现**（补完）：上一版把 JS 和 CSS 在算哈希前统一了行尾，
  但品牌资源按原始字节喂进哈希，其中两个 SVG 是文本文件。`.gitattributes`
  钉死了 `.js/.css/.html/.md` 却漏了 `*.svg`，autocrlf 机器 checkout 出来是
  CRLF（1153→1171 字节），两台机器算出两个戳，pull 下来一构建就变脏。
  两个 SVG 改为归一化后入哈希，并补上 `*.svg text eol=lf`

---

## 2026-08-06 · 补齐对标竞品的短板，但不抄它的编造

这一轮的起点是一次竞品对照。结论是：**功能差距比想象中小，真正的差距是
"我们对的地方用户感受不到，我们弱的地方用户第一小时就撞上"**。

- 发送安全是我们压倒性领先的地方，但它防的是三个月后域名进黑名单——
  用户试用时只体验到"这软件老拦着我不让发"
- 不编造联系人是我们花一整轮做对的事，但界面上显示的是「待查联系人」，
  而竞品那一栏写着「John Smith · john.smith@acme.com」

所以这轮做两件事：**把已有的优势变得看得见**，**把最致命的短板补上**。
竞品软文里那些"ROI 1:2300""回复率提升 40%"一个都没抄——那是我们上一轮
刚清理掉的东西。

### 🌐 官网抓取：一条一直没走的路

绝大多数中小外贸买家的官网上，直接写着 `info@` / `sales@` / `purchasing@`。
**抓下来就行——100% 真实、完全免费、不违反任何原则。** 我们一直在为
"找不到联系人"发愁，却没做这件最简单的事。

- 首页 → 找 contact/about/team 内链 → 抓取 → 提取 `mailto:`、明文邮箱、
  以及 `name [at] domain [dot] com` 这类防爬写法
- **每个邮箱都留 `source_url`**，点开就能核对——不能溯源的联系方式等于没有
- 采购类信箱排最前（`purchasing` > `sales` > `contact` > `info` > 个人名字信箱）
- 尊重 `robots.txt`，UA 如实标明身份不伪装成浏览器，并发压到 3
- 顺带抓电话、领英/脸书/WhatsApp
- 插在 Hunter **之前**：免费、不要 key、是企业主动公示的一手信息

实测 gnu.org 抓到 5 个真实邮箱、sqlite.org 抓到 1 个（并正确标为非同域）。

### ✅ 邮箱存在性验证：问对方服务器，不猜

连 MX 的 25 端口，走到 `RCPT TO` 就停——不发信、不打扰任何人。

- 同时探一个随机地址：随机地址也说"存在"就是 catch-all 域名，
  这时**必须报"测不出"**而不是"有效"
- 家用宽带常封出站 25 端口。封了就老实说"探测不了"，
  **绝不把探测失败当成"地址无效"**——那会误杀真客户
- 探测结果接进 F3 判定，且**优先级高于来源可信度**：
  官网上印错邮箱、写的是停用地址，这些都真实发生

> 这件事**本地应用比云端 SaaS 更做得成**，这点一直被低估：
> 云端集中发起 RCPT 探测和官网抓取，几万用户共用几个出口 IP，很快会被
> 拉黑，所以云端产品要么不做要么买第三方。我们从用户自己的 IP 出发，
> 一天几百次完全在正常范围内。

### 🛡 发信护航：把隐形的保护变成看得见的数字

我们一直在拦、在限速、在挡不合规的信，但这些全是隐形的。

- **已拦下 N 封**——"来源存疑 + 探测确认不存在。这些如果发出去，就是 N 次退信"
- **你的退信率**——对比行业均值，超 5% 降权、超 10% 有封号风险
- **域名预热**——第几天、今天还能发几封，以及**为什么**要压着发
- **SPF / DKIM / DMARC 一键体检**，每项给出"必须修/建议改/正常"和具体怎么改

> **体检这件事差点做成有害的。** 实测发现国内 DNS 会返回残缺的 TXT 记录：
> 查 github.com，系统默认 DNS 8 条（无 SPF）、8.8.8.8 22 条（有 SPF）、
> 阿里 223.5.5.5 17 条（无 SPF）。如果据此告诉一个**已经配了 SPF** 的用户
> "你没有 SPF，去加一条"，他加上第二条之后按 RFC 7208 直接判为 permerror
> ——**我们会亲手把他的送达率搞坏**。
> 现在并行问多个解析器取并集，问不到可信解析器就明说「查不准」，
> 并且查不准的项不计入评分——把"没查到"算成"不合格"是另一种编造。

### 📮 十分钟发出第一封

我们最致命的短板：竞品注册完输个 HS 编码就出结果，我们要配 key、定位、
自己去搜、粘回来、配 SMTP、还要熬 14 天预热。试用只有 20 条线索，
用户很可能在发出第一封之前就把耐心耗光了。

控制台新增五步向导，每步都显示**还差什么**和**点这里做**，全部完成后自动消失。

### 📈 行为驱动的跟进 + 打开次数

固定 7-14-30-60 天的节奏对所有人一视同仁，但"打开 5 次没回"和"一次没打开"
显然该走不同的路。

- 八条分支：已回复 / 退信 / 自动回复 / **打开≥3次未回（意向最强）** /
  打开1-2次 / 没有打开数据 / 连续未打开 / 刚发出
- 打开事件从布尔值改成**累加次数**——布尔值表达不了"打开 3 次"和"打开 1 次"的区别
- 连续未打开自动暂停序列：既提效果，也是护航的延伸

> **这里差点埋一颗雷。** 没配追踪时 `opens` 恒为 0，`cold` 分支会把
> **所有人**判成"发了 3 封没打开"并自动暂停全部序列。
> 现在 `cold` 必须要求确实有追踪数据——**测不到 ≠ 没打开**，这是两回事。

### 🎯 开发信引用官网上的真实事实

竞品靠海关记录做个性化（"你 3 月进口过 X"），但那只对有海关记录的公司有效。
官网人人都有，而且引用对方官网上的具体业务描述更难被识破为模板。

只摘录官网原话，不概括、不推断、不润色成"我们深入研究了贵司业务"。
每次引用都记下用了哪条事实、出处是哪个 URL。

### 🔁 定位闭环校准

我们的产品定位（HS + 同义词 + 买家段 + 排除词）比竞品细一档，但它是单向的：
AI 产出定位 → 拿去搜 → 完事，没有人告诉定位"你搜回来的东西对不对"。

现在从最近入池的公司里抽几家让用户点「对口 / 不对口」，不对口的把行业特征词
喂回排除词。**这不是"AI 学习"，就是把用户的明确判断记下来并用上。**

> **又一颗差点埋下的雷。** 用户卖无人机配件，标记一家无人机工厂为不对口时，
> 原实现会把 `drone`、`quadcopter` 收进排除词——下次搜索式会把所有无人机相关
> 公司全排掉，**等于亲手废掉他的搜索**。现在先把用户自己的产品词、同义词、
> 买家段词全部保护起来，一个都不许进排除词。修完后同样的输入只排掉
> `factory / manufacturer / supplier`，正是"这是工厂不是买家"的信号。

### 🔒 追踪像素挂在你自己的域名上

竞品的追踪像素挂在**他们的**服务器上——等于把你所有客户的打开时间和 IP
交给服务商。这里的端点由你填，追踪数据同样不经过我们。

**不填就是没有打开数据。** 我们不会给一个"预估打开率"——那又是一种编造。

### ✅ 验收

自动化测试 92 → **111 项**，全绿。新增 `test-netprobe.mjs`（19 项），
其中三条钉的是上面三个"差点埋下的雷"。

| 套件 | 项数 |
|---|---|
| `test-send-guard.mjs` | 21 |
| `test-netprobe.mjs` 🆕 | 19 |
| `test-mail.js` | 19 |
| `test-customs.js` | 13 |
| `test-parse.mjs` | 11 |
| `test-import.mjs` | 11 |
| `test-license.mjs` | 10 |
| `test-desktop.js` | 7 |

### 🚫 明确不做的两件事

- **自带买家数据库**（竞品主打 3 亿条海关记录）——持续采购和更新是订阅制
  才养得起的成本结构，买断制养不起。我们支持导入你自己的海关数据。
- **多账号 / 团队协作**——单机买断，做多账号等于重做架构。

> 说清楚"我们不做什么"本身是有说服力的。什么都说自己强的产品，用户反而不信。

---

## 2026-08-05 · 去掉一切编造与夸大

这一轮的主线只有一条：**产品不能给用户假数据、也不能给用户假预期**。
起因是几条真实反馈——AI 编出一个不存在的采购经理、界面上挂着"78% 成交概率"、
粘贴导入报错却不说卡在哪。逐条查下来，发现问题多数不在 AI，在我们自己的代码里。

### 🚫 不再编造联系人

**根因是我们自己写在提示词里的。** 原 schema 对 `contact_name` 的描述原文是
"推测的最可能对口决策人姓名（英文），**拿不准就给常见采购负责人占位**"——
那个不存在的 "Ahmed Al-Mansour" 不是模型幻觉，是我们命令它编的。用户会把这个名字
写进真实开发信的开头。

- **AI 纯推测整条删除**：原来"凭公司名和域名推测决策人 + 按 `firstname.lastname`
  拼候选邮箱"这一步已移除，替换为**联网核实**（真的去翻官网 About / Team / Contact 与领英）。
  模型不支持联网时不再退回推测，而是明说"没编造，仍是待查找"。
- **本地规则兜底不再造数据**：原来会从 12×12 的名字库拼人名、按域名拼邮箱、按市场编电话号码。
  这三样全部留空，只保留"不需要编就成立"的字段（岗位方向、采购信号措辞）。
  同时去掉了 `confidence + 18`——那是在给编造出来的数据加分。
- **联网核实这条路也收紧**：原提示词里有一句"确实找不到就按该域名常见模式给候选并标 guessed"
  ——等于允许它编。改成找不到就返回空数组，并要求每个邮箱给出 `source_url`。
  代码侧再加一道闸：`claude-web` 来源的邮箱，没标 verified 又没出处的一律丢弃。
- **删掉 56 行造假代码**：`ENRICH_FIRST_NAMES` / `ENRICH_LAST_NAMES` / `buildEmailCandidates`
  / `makePhoneNumber` 全部移除（确认零引用）。删掉比留着好——留着就有被重新用上的一天。
- **清理存量假数据**：改代码不会清已经落盘的数据。新增一次性迁移，启动时按"推测来源"
  （`contactSource` 为 claude/local，或候选邮箱标着 firstname/functional/guessed）
  清掉人名、邮箱、电话。真实源、联网核实、粘贴导入的原始邮箱、客户回过信的地址一律保留；
  已回复客户与回信导入线索整条跳过。清理后在日志里说明清了几条、为什么、去哪拿真联系方式。

现在联系人只剩两个合法来源：**真实源**（Hunter 直连 / 邮箱查找 Webhook）与
**AI 联网核实**。都拿不到就是「待查联系人」——这是正确状态，不是缺陷。

### 📉 不再承诺商业成果

界面上挂着「AI 成交概率 78%」，而软件的免责声明白纸黑字写着
"不对任何商业成果（回复率、询盘量、成交额）作出承诺或保证"。两者直接打架，
真出纠纷用户完全可以说"你的系统显示 78% 成交概率"。

- 「AI 成交概率」→「**线索优先级**」；`78%` → `78 分 · 用于排序，不预测成交`，
  **百分号去掉**（带 % 天然读作概率），并加一行说明"这是排队顺序，不是成交概率"。
- CRM 卡片 tooltip 同步改口径；评分函数上方写死注释，防止以后被改回去。
- 保留 CRM 看板的「成交率」——那是**历史统计**（已成交/总商机），报事实不是预测。

### 🎯 评分不再自相矛盾、不再有假精度

- **AI 判定不对口的线索直接沉底**：出现过「AI 说完全不对口 0%」但质量分仍是「B 78」的情况。
  现在 `offTarget` 扣 45 分、`fitScore < 60` 扣 12 分。实测那条不对口的：**7 分 · D 级**。
  并且不再为它补联系人和邮箱，批量入队与自动驾驶都跳过（单条手动入队仍可以，判断权在人）。
- **`scoreProspect` 去掉假精度**：原来是 `来源基础分 + 市场名长度%11 + 序号%6`。
  后两项和线索质量毫无关系——同一家公司只因为市场名多几个字符就凭空多 7 分。
  这个分会进质量分、决定排序、显示给用户。只保留真正有信号的来源渠道。
- **「邮箱已验证」名不副实**：它判的只是**格式**，而 F3 里的"已验证"是严格口径
  （真实源 / 客户回过信 / 人工核实）。同词两义会让用户以为这条已经能发了。
  改成「**邮箱格式有效**」，注明"只校验了写法，未确认这个地址真实存在"。
- **「源评分很高 · 引擎判断为高价值线索」是夸大**——引擎根本没看过这家公司，
  它只知道线索来自哪个渠道。改成「来源渠道可信度高 · 海关提单这类来源，
  本身就说明对方有真实进口行为」。

### 🔍 粘贴导入：两个真 bug + 失败不再哑巴

用户报"解析线索·已中止，这段内容里没有公司官网或邮箱可用"，但不知道卡在哪。查下来：

- **域名兜底扫描漏掉所有裸域名**。正则 `(?:\.[a-z0-9-]+)+\.[a-z]{2,}` 里那个 `+`
  要求至少三段，`www.acme.com` 能匹配、**`acme.com` 匹配不到**。
  于是"散文里夹着域名"这条兜底形同虚设——`visit us at gulfagri.ae` 解析结果是 0 条。
- **认不出现在的 Google 结果页**。判断"是不是搜索结果"用的是 `https:// | www. | @域名`，
  但 Google 现在复制出来是 `gulfagri.ae › products`——三者都没有。判定为否 →
  "无联系方式的行一律丢弃"这条保护不生效 → 导航文字全被当成公司。
  实测整页粘贴出 13 条，其中 10 条是「搜索/图片/视频/下一页/设置/隐私权」。
- 顺带修两个误伤：`report.pdf` 被当成公司官网（行解析正则会吃文件名，已加顶级域闸）、
  整句散文被当成公司名（没有联系方式时，带句号或超 8 词的行直接丢弃）。
- **失败分型**：同样是 0 条，原因完全不同，现在分别告知——
  粘的是搜索式本身（最常见）／全是平台社媒站／池里早就有／压根没有域名。
  同时把各步筛除统计和原文前 80 字写进诊断环形缓冲，售后能直接看到当时粘了什么。

### 🖼 UI 对齐：写脚本逐个量，不靠肉眼

对 12 个视图的每个按钮和勾选框做几何测量（图标中心 vs 文字中心 vs 容器中心）：

- **按钮文字被表单标题样式劫持**。`label span { display:block; margin-bottom:7px;
  font-weight:800; color:muted }` 是给字段标题用的，但「AI 细化定位」等按钮长在
  `<label>` 里，文字 span 也被命中：7px 下边距把文字顶高 **3.5px**（图标居中、文字偏上），
  颜色被染灰、字重拉到 800，蓝底上尤其明显。改成 `button span` 不吃这套。
- **勾选框被撑成 16×40**。全局 `input { min-height:40px }` 是给文本框写的，
  `height:16px` 压不住 `min-height`，方框中心比同行文字低 **11px**。
  上一轮按选择器逐个修，结果发信队列的行勾选框漏掉了——这次改成对所有
  checkbox/radio 全局钉死尺寸，根治这一类。
- **潜客页横向溢出 24px**。`.topbar-actions` 被顶栏和各页标题行共用，
  给顶栏设的 `nowrap` 把潜客页 6 个操作按钮也锁成不换行，最后一个被顶出视口。
  收窄成 `.topbar > .topbar-actions` 才 nowrap。
- 复测：1150（Electron 最小宽度）与 1280 下，12 个视图**横向滚动 0、未同心 0、撑大 0**。

### ⚙️ 其它

- **设置页保存不用滚回顶部**：底部常驻保存条（sticky）＋ **Ctrl/Cmd+S**；
  改过任何输入框，保存条变琥珀色并提示"有改动还没保存"。
- **运行状态条终态不再跟着你逛全站**：跑着的时候全局常驻（长任务切页要能看见），
  但「已完成/失败/中止」是终态——你一主动切视图就自动收起。
- **搜索式面板加说明**：标题改为「**搜索式（要你自己去搜）**」，第一句写明
  "这些不是已经找到的客户"，并讲清它和「联网找客户」是两条完全不同的路。
- **AI 联网返回解析加固**：原来"第一个 `[` 到最后一个 `]`"的朴素切片，
  遇到引文标记 `[1]`（本身就是合法 JSON 数组）会误取、遇到 `max_tokens` 截断会全废。
  改成三级兜底：代码围栏 → 括号配平扫描 → 从截断文本里逐个捞完整对象；
  web search 的 token 上限 4500 → 8000；失败时分"被截断"与"没按格式答"两种提示。
- **桌面快捷方式**：新增 `启动觅客舵.vbs` 与桌面 `.lnk`，双击直开、无控制台闪烁；
  启动前静默重建 app.js，保证 `src/` 的改动生效。

### ✅ 验收

自动化测试从 36 项增加到 **92 项**，全绿：

| 套件 | 项数 | 覆盖 |
|---|---|---|
| `test-send-guard.mjs` | 21 | 邮箱验证判定、试用锁定态、合规、脱敏 |
| `test-license.mjs` | 10 | 激活码签发验签 |
| `test-parse.mjs` 🆕 | 11 | AI 联网返回的公司列表解析（引文标记、截断救援、转义） |
| `test-import.mjs` 🆕 | 11 | 粘贴导入（裸域名、整页粘贴、误伤、失败分型） |
| `test-customs.js` | 13 | 本地提单库 |
| `test-mail.js` | 19 | 内置 SMTP/IMAP |
| `test-desktop.js` | 7 | 真 Electron：机器码 / safeStorage / 备份滚动 |

> 两个新测试在编写过程中当场各抓出一个漏洞：`[1]` 被当成合法公司列表、
> `acme.com` 扫不到。这正是为什么它们值得存在。

---

## v1.0.0 · 2026-08 商业化版本（更名「觅客舵」）

本轮**不加任何新业务功能**，只补收款前必须的商业化基建。业务功能与上一版逐条一致。

### 🔑 F1 离线激活码系统
- 机器码 = CPU ID + 主板序列号 + 系统盘卷序列号 → SHA-256 前 16 位，按 `XXXX-XXXX-XXXX-XXXX` 展示并一键复制。
- 激活码 = Ed25519 私钥对 `机器码|档位|签发日` 的签名，base32 编码为 14 段 × 8 位；公钥硬编码进应用，**全程不联网**。
- 验证通过后用 Electron `safeStorage` 加密写入 userData；文件损坏或被改 → 静默回退试用模式，不崩溃。
- 失败分型明确：格式错误（提示 O/0、I/1）与验证失败（提示机器码不匹配）两套文案；连续 3 次失败露出客服微信。
- 开发者侧 `tools/gen-key.mjs` 一条命令出码并记发码流水，`tools/init-keys.mjs` 生成密钥对并自动回写公钥。

### 🧪 F2 试用模式
- 线索池**存量**硬上限 20 条。粘贴导入 / 联网找客户 / 一键起量 / 找相似客户 / 周期补量 / 采集 Webhook / 演示采集，全部经同一个容量闸门。
- 第 21 条弹升级墙，三个按钮：查看价格 / 输入激活码 / **先删几条腾位置**（被墙时留个台阶）。
- 顶栏常驻角标 `试用版 · 线索 14/20`，点击直开激活弹窗。
- 试用版云端大模型区显示锁定态（不隐藏，让用户看见正式版有什么）；导出文件加试用水印行。
- 激活瞬间三联动：角标消失、水印消失、AI 区解锁，**不用重启**。

### ⛔ F3 未验证邮箱强制拦截（口碑保命）
- 邮箱来源为规则/AI 推测且未验证的线索，**入队与发送两个环节全部拦截**，一键入队、批量审批、自动驾驶无一例外。原本只是 ⚠ 提示的这一项，升级为 ⛔ 阻断。
- ⛔ 徽章改为**可点击**，展开原因面板：解释在保护什么，并直给三个修复入口（立即验证这条 / 批量验证全部待验证 / 我确认无误，标记人工核实）。
- 「标记已人工核实」走危险样式二次确认，永久留痕（时间 + 操作者），数据导出可见；核实只对被核实的那个地址生效，换候选邮箱重新拦。
- 潜客列表新增「⛔ 待验证」筛选，一眼看到堵在哪；潜客详情新增「发信资格」一行。

### 🩺 F4 诊断日志与错误兜底
- 主进程 / 渲染进程 / unhandledrejection 全捕获，崩溃不白屏，错误页第一句先说"你的数据是安全的"。
- 最近 200 条操作环形缓冲 + 错误堆栈 + 各数据表条数 + 系统与版本信息，一键导出。
- 脱敏：邮箱按 `a***@d***.com` 打码、电话只留国家码与末两位、**绝不包含邮件正文**；文件小于 2MB。
- 设置页与「帮助」菜单常驻导出入口，不止崩溃时可用。

### 🔄 F5 自动更新
- electron-updater + GitHub Releases；有新版本时顶栏出蓝点，不打断当前操作；更新后首启弹更新日志。
- 激活签发超 365 天：检查更新提示续费 ¥199/年，**已装版本照常使用**。

### 🧭 F6 新手引导对齐课程
- 五步与课程同构：配基建 → 定位 → 灌线索 → 发首批 → 处理回复；当前步展开"做什么 / 为什么 / 做完你会看到"。
- 进度持久化，可手动打勾，也可从「帮助 → 重新打开新手引导」重来；每步带「配套课程」入口。

### 💾 F7 数据自动备份
- 每天首次启动自动把全量数据导出到 `userData/backups/`，滚动保留 14 份。
- 恢复流程：选文件 → 预览数据规模（备份里 vs 当前）→ 确认覆盖；**覆盖前自动再备份一次当前数据**，选错还能退回来。

### ⚖️ F8 合规与限流
- 目标市场含欧盟/英国的活动：开发信模板级注入退订说明，删掉后发送预检直接 ⛔；建活动时一次性 GDPR 提示（陈述事实，不恐吓）。
- 新发信域名前 14 天日发**硬顶 20 封**（软件卡合规底线；课程教第 1 周 ≤10 封是最佳实践，两者并存不冲突）。预热起点按第一封真实发出的邮件反推，改配置绕不过去。
- 发信队列顶部新增**起航航程条**：D1–D14 虚线航线 + 航标橙小船 + 今日限额，把"被限制"翻译成"在航行"；满 14 天收成顶栏一枚绿锚。
- WhatsApp 冷发（无邮件触达记录直接发）每次弹风险确认，不可关闭；非冷发的部分照常发出，不被一次确认卡住整批。

### ✅ 与设计文档逐条对齐（D 部分七个页面 + B 部分十节 + F 部分走查清单）

对着《产品设计文档 v2.0》A–F 全章节核了一遍，把缺口全部补齐，逐条结论见 [docs/设计文档核对表.md](docs/设计文档核对表.md)。

**核对中发现并修掉的三个真问题**
- **人工核实留痕导不出去**：`toCsv` 用第一行的字段名当表头，第一条线索没被核实过，整列就从 CSV 里消失，后面核实过的留痕跟着丢。改为取全行字段并集，并把留痕摊平成 `sendEligibility / manualVerifiedBy / At / Email` 四列——F3「人工核实标记数据导出中可见」这条验收原本是不成立的。
- **试用水印插到了 UTF-8 BOM 前面**，Excel 认不出编码，导出的 CSV 中文全乱码。
- **数据目录名不对**：`package.json` 只有 `build.productName`，Electron 读的是顶层字段，数据落在了 `%APPDATA%\mikeduo`。补了顶层 `productName`，现在是 `%APPDATA%\觅客舵`——这个必须发货前定死，上线后再改老用户数据就"消失"了。

**补齐的功能缺口**
- **F6 自发自收**：引导第 4 步主按钮改成「先发一封给自己」——填自己的邮箱后建一条自测线索（标真实源，不被 F3 拦、不占试用额度），走真实发送链路发出，正文直接写清"进收件箱还是垃圾箱各代表什么、下一步去 mail-tester 测几分"；本地模拟模式会明确告知"这只是模拟"。补上了那条一直不成立的验收。
- **D1 漏斗速览**：线索→验证→触达→回复→询盘 五格，格间标环节转化率，点任一格直达对应页。
- **D2 潜客队列全量对齐**：质量分 A–D 分段控件、来源/验证状态/市场三个筛选、行选择框、联系人+来源三色徽章列、操作文字按钮列、**选中即滑入的底部批量操作条**（并提示其中多少条未验证）、**行点击开 420px 详情抽屉**、**待验证置顶红条**、质量分徽章点开看因子明细。
- **D3 发信队列**：预检徽章挪到最左列、主按钮实时显示数量、邮件行内展开预览、主题超 60 字符出琥珀提示。
- **D4 收件箱三栏**：AI 面板独立成 320px 第三栏并 sticky，高风险红卡置顶；会话列表加 🔥热/🌤温/❄️冷 意向胶囊；**首个买家回复到达时脉冲 400ms**。
- **D5 CRM 卡片**：加质量分微徽章，下次跟进日期改为常显。
- **D7 设置页左锚点导航**：六个分区，sticky 且随滚动高亮。
- **B6 补全**：40px 按钮档、带撤销的 Toast（移入黑名单可 6 秒内撤回）、空状态插图、列表骨架屏、一键起量的顶部细进度条＋右下角可折叠滚动日志、弹窗焦点圈定、危险弹窗遮罩点击不关、禁用控件自动补 tooltip。
- **B3/B4/B8 补全**：来源三色描边徽章、意向色规、API Key 等宽字、开发信预览 15/24 排版、全局图标 1.5px 描边、品类四卡 32px 双色图标。
- **F8**：GDPR 提示加「以后不再提示」勾选框。

### 🎨 版式重做（B 部分：密度优先 / 状态即信息）

第一版只换了色板，界面骨架还是老的——顶栏散成四行、侧栏折叠掉品牌、卡片留白比内容多。这轮按 B/D 部分把框架层重做了：

- **顶栏收成一行 52px**：左边新增**活动切换器**（当前活动名 + 下拉，列出各活动线索数，可直接切换或新建），右边是紧凑图标组 + 自动驾驶 + 试用角标 + 唯一主按钮。删掉了跟侧栏重复的大标题。
- **侧栏 216px**：品牌区收紧，导航项 34px，选中态用深一档底色 + 左侧一条航标橙细标；分组标签 micro 大写不换行；窄屏（<1150）整体退成 64px 纯图标。
- **控制台改为待办优先**：「今日待办」提到最上并改成**横向卡组**（大数字 + 说明 + 动作），配置卡下沉——老用户每天来是处理事，不是改活动。
- **上手五步改成横向步骤条**：一行看完进度，只展开当前一步的"做什么 / 为什么 / 做完你会看到"，可点任意节点跳看、可收起、可不再显示。原来那个竖排五张卡占掉大半屏。
- **潜客表格重做**：行高 40、斑马纹、悬停变色、选中行左侧 2px 主色条；列改为 公司 / 市场 / **质量分（字母+分值合成一枚徽章）** / **发信资格** / 状态——"这条现在能不能发出去"直接进列表，不用点开详情。
- **发信队列加预检三色分段**：`✓ 4 可发送 ｜ ⚠ 1 提示 ｜ ⛔ 16 阻断`，**点数字即筛选**；队列本身从三等分栏改为主栏 2:1，条目一行一条不再被挤成竖排。
- **字阶收敛到 6 档**（22/16/14/14粗/12/11），表格与数字统一 tabular-nums；按钮 32 高 6 圆角、徽章 4 圆角、卡片 8 圆角单档阴影；输入框聚焦主色 2px 环。
- 顺手收编了几处没走变量的旧品牌绿（星标指标卡、Toast、欢迎卡图标、优先联系名单），深色模式下把 A 级质量分改回绿色——绿只表示"好"，不能被主色顶掉。

### 🎨 F9 品牌化收尾
- 更名「觅客舵 MiKeDuo」：应用名 / 窗口标题（`觅客舵（试用版） · 活动名`）/ 侧栏品牌标 / 安装包名 `MiKeDuo-Setup-v1.0.0.exe` 全部统一。
- 图标重做：深海蓝底 + 白色舵轮线标，`tools/make-icon.mjs` 纯 JS 栅格化生成 16/32/48/256 四档，16px 档手动简化为外环 + 四辐。
- 视觉令牌按远洋航务感重映射：深海蓝侧栏 `#0F2B45`、航路蓝主色 `#155E93`、航标橙 accent `#E2571E`（每屏至多一处）；表格数字统一 tabular-nums；深色模式同步。
- 关于页含版本、激活状态与档位、机器码、更新到期日、客服微信、《许可与免责声明》入口；首次启动强制勾选声明。

### 🧰 工程
- 新增 `preload.js` + `electron/` 主进程模块，渲染层通过 `window.mkd` 拿机器码、加密存储、备份目录、原生保存对话框与更新能力；浏览器直开时自动降级（激活须在桌面版）。
- 新增 `npm test`：26 项单测（激活码 10 项 + sendGuard/试用容量/合规/脱敏 16 项），直接在 vm 里跑真源码，不复制一份逻辑来测。
- 商业化代码集中在 `src/0-brand-edition.js`（常量与纯函数，先于一切初始化）与 `src/08-commerce.js`（DOM 与增强），对既有业务函数一律"包一层"，不散落改动到 01–07。

## 2026-07 本轮优化

### 🚀 性能与工程
- **按视图渲染**：`render()` 只重建当前可见视图，不再每次操作都重建全部 12 个视图（大列表下明显更跟手）。
- **持久化防抖**：多次连续改动合并成一次写盘，并在关闭/隐藏页面时兜底落盘，既省开销又不丢数据。
- **筛选框输入防抖**：潜客/搜索/会话筛选连续输入合并渲染，不再每敲一个字符就重建整张列表。
- **模块化构建**：`app.js` 拆成 `src/` 下 8 个模块，`build.mjs` 拼接生成（逐字节等价），自动打版本戳防浏览器缓存旧版。

### 🤖 AI 引擎
- **多服务商支持**：新增「云端大模型」——Claude / OpenAI ChatGPT / DeepSeek / 通义千问 / Kimi / 智谱 GLM，或「自定义」填任意 OpenAI 兼容接口。默认省钱用 DeepSeek，Claude 仅联网功能需要。
- **上下文喂足**：写信/回复提示词自动带上具体产品定位、术语、HS 编码、买家画像、客户类型与**产品知识库**，输出更贴产品、不编造（此前回复建议根本没带知识库）。

### 🧭 更好上手 / 日常提速
- **首次欢迎向导**：新用户第一次打开给两条明路——「一键体验」用演示数据跑到发信队列看效果，或「填我的产品直接开始」。
- **今日待办工作台**：控制台顶部聚合每天要处理的事（待审批/到期待发/新回复/到期跟进/Agent 待审批/超期未备份），一键直达或直接处理；Webhook 模式下还可一键「拉取新回复 / 拉取送达状态」。
- **优先联系名单**：数据分析看板按机会分排出「今天先追谁」Top5（已回复 > 已打开 > 高分），点一行直达该客户。

### 📨 发信域名保护（到达率）
- **退订说明**：所有开发信（模板 + AI 写信）自动附一句轻量退订说明，回复 unsubscribe 即被识别退订并拉黑。
- **送达/退信回传**：新增「发送状态回传 Webhook」，同步 送达/打开/退信/投诉；**硬退信与投诉自动进黑名单**，不再往坏地址发。
- **退信率警告**：数据分析看板显示退信率，样本≥10 且≥5% 时红字提示先停量。
- **垃圾词预检**：发信预检徽章扫描易进垃圾箱的词/排版（guarantee、act now、click here、$$$、连续感叹号、多个全大写词……，排除 FOB/MOQ 等外贸缩写），命中提示改写。

### 💼 报价转化包（询盘 → 报价 → 成交）
- **产品库**：管理页维护常卖型号（型号/英文品名/MOQ/参考价/单位/箱规/认证），一处维护三处受益——AI 回复引用真实参数、报价单直接选行、开发信卖点更准。
- **报价单生成器**：CRM 页「生成报价单」——选客户 + 勾产品行（或加自定义行）+ 贸易条款/币种/有效期 → 生成专业英文报价单（自动编号 Q-日期-序号），可**打印/存 PDF** 或**一键复制文本版**粘进回信。
- **CRM 联动**：生成即自动推进到「报价」阶段、管道金额换成真实报价额；3 天后引用报价编号的跟进邮件自动排队（待审批）——报价不再石沉大海。

### ⏰ 发送时机（按市场时区）
- 发信队列每封待发邮件显示**发送时机徽章**：客户当地此刻几点、最佳发送对应的北京时间段（当地上午 9-11 点）；当地正值上午显示"现在发正合适"，深夜则提醒换时段。
- Webhook 发信 payload 附带 `recipient_utc_offset / recipient_local_time / suggested_window_beijing` 字段，n8n 可按此定时投递，开信率更高。覆盖 40+ 常见市场（不处理夏令时，按代表时区近似）。

### 📊 AI 每周战报
- 分析页「生成周报」一键出本周摘要：触达（发送/送达/打开/退信）、回复与热意向、报价张数与金额、管道快照、待办，附本地规则的下周行动建议，可**一键复制**汇报。
- 配置 AI 引擎后自动追加大模型的针对性点评（失败自动降级本地建议）。

### 💾 数据安全
- **老邮件瘦身**：设置「数据与备份」可一键把 30 天前发出、客户未回复的已发邮件正文归档清空（分析统计不受影响），缓解 localStorage 5MB 上限。
- **备份进待办**：超 7 天未备份直接在今日待办出一行，一键导出 JSON。

### 📦 交付与文档
- **桌面应用**：Electron 免安装绿色版 + 浏览器版双入口，数据互通。
- **文档**：新增 `上线执行清单.md`（含 7 个 Webhook 接口规格 + DNS 配置 + CORS 说明）、`n8n自建说明.md`、`src/README.md`；更新 `README.md`。
